--- /dev/null	2026-09-02 03:15:53
+++ docs/requirements.md	2026-09-02 03:10:52
@@ -0,0 +1,482 @@
+# Telegram 影视 Mini App 产品需求文档
+
+## 1. 文档信息
+
+| 项目 | 内容 |
+|---|---|
+| 文档版本 | 1.0 |
+| 文档状态 | 需求基线 |
+| 更新日期 | 2026-09-02 |
+| 产品形态 | Telegram Bot + Telegram Mini App + 管理后台 |
+| 当前阶段 | MVP 规划 |
+
+## 2. 项目背景
+
+本项目面向 Telegram 用户提供影视、照片、漫画和写真内容的发现、浏览与分发能力。Mini App 负责内容目录、搜索、作品详情、图集浏览和漫画阅读；影视文件存放在 Telegram 私有存储频道，由 Bot 发送至用户私聊，并使用 Telegram 原生播放器播放。
+
+本方案的核心目标是在保留良好浏览体验的同时，避免 VPS 永久存储影视文件及承担影视播放流量。
+
+## 3. 产品目标
+
+### 3.1 核心目标
+
+1. 用户可在 Mini App 中浏览、搜索和收藏多种媒体作品。
+2. 用户可在 Mini App 中查看普通图集、写真和漫画。
+3. 用户选择影视内容后，由 Bot 将受保护的视频发送至用户私聊并使用 Telegram 原生播放器播放。
+4. 影视和图片原始资源主要存放在 Telegram，不永久存储在 VPS。
+5. 当前作品目录仅展示实际存在、已发布且用户有权访问的内容分区。
+6. 管理员可通过私有频道入库媒体，并在管理后台完成作品编排和发布。
+
+### 3.2 成功指标
+
+MVP 上线后重点观察：
+
+- Mini App 首次可交互时间和页面加载成功率。
+- 搜索到作品并完成播放发送的转化率。
+- Bot 视频发送成功率及平均发送耗时。
+- 图集首图加载时间、漫画翻页成功率和图片错误率。
+- 收藏、继续阅读和再次访问比例。
+- 被限流、资源失效和权限校验失败的比例。
+
+### 3.3 非目标
+
+MVP 不包含：
+
+- Mini App 内嵌视频播放器。
+- HLS、MTProto 流媒体网关或 VPS 视频代理。
+- 精确到秒的视频播放进度同步。
+- DRM 级别的内容保护。
+- 离线视频下载、投屏、弹幕和多音轨播放器。
+- 面向公开互联网的独立影视播放网站。
+
+## 4. 用户与角色
+
+### 4.1 普通用户
+
+- 通过 Telegram 身份进入 Mini App。
+- 浏览、搜索和收藏作品。
+- 阅读漫画，浏览图集和写真。
+- 请求 Bot 发送影视内容。
+- 查看最近阅读和最近发送记录。
+
+### 4.2 受限或会员用户
+
+- 具备指定作品、分区或内容单元的访问权限。
+- 权限到期后不能请求发送新的受限资源。
+- 已发送消息的可访问性受 Telegram 能力限制，详见内容保护边界。
+
+### 4.3 内容管理员
+
+- 创建和维护作品资料。
+- 将 Telegram 存储消息关联到作品、剧集、章节或图集。
+- 调整内容顺序、发布状态和访问权限。
+- 查看资源入库状态和发送失败记录。
+
+### 4.4 系统管理员
+
+- 管理管理员账号和角色。
+- 配置 Bot、存储频道、发送策略和资源保护策略。
+- 查看审计日志、系统状态和任务队列。
+
+## 5. 已确定的产品原则
+
+1. Mini App 是内容浏览与图片阅读入口，不承担影视流媒体播放。
+2. 影视播放采用 Telegram 原生播放器。
+3. 视频通过 Bot 从私有存储频道复制或复用后发送至用户私聊。
+4. 所有面向用户发送的媒体消息必须启用 `protect_content`。
+5. 作品是内容容器，但必须有明确的主类型和允许的附属内容。
+6. 导航分区由实际发布内容动态生成，前端不得固定显示空分区。
+7. 漫画作品不包含视频内容。
+8. 影视作品可包含剧照或海报图集。
+9. 写真作品可包含拍摄花絮视频。
+10. 普通照片、写真和漫画使用不同的浏览逻辑，不强制共用同一种阅读体验。
+
+## 6. 内容模型与组合规则
+
+### 6.1 作品主类型
+
+| 主类型 | 子类型示例 | 主内容 | 允许的附属内容 |
+|---|---|---|---|
+| 影视 | 电影、电视剧、短剧、动漫视频 | 视频或选集 | 剧照、海报、幕后图集 |
+| 漫画 | 连载、完结、短篇 | 漫画章节 | 封面、插图；不允许视频 |
+| 图集 | 普通照片集、主题图集 | 图片集合 | 补充图片 |
+| 写真 | 单辑、多辑写真 | 写真图集 | 拍摄花絮视频、幕后照片 |
+
+### 6.2 内容层级
+
+```text
+作品
+  -> 内容分区
+    -> 内容单元
+      -> 媒体资源
+```
+
+- 作品：名称、封面、简介、标签、主类型等聚合信息。
+- 内容分区：选集、剧照、漫画目录、写真、花絮等导航单元。
+- 内容单元：一集视频、一个漫画章节、一套写真或一个剧照图集。
+- 媒体资源：最终的 Telegram 视频消息、图片文件、封面或缩略图。
+
+### 6.3 动态分区规则
+
+分区仅在同时满足以下条件时展示：
+
+1. 当前作品允许该内容类型。
+2. 分区中至少存在一个已发布内容单元。
+3. 当前用户至少有权查看分区中的一个内容单元。
+
+示例：
+
+| 当前作品内容 | 展示分区 |
+|---|---|
+| 电影，无剧照 | 详情、播放 |
+| 电视剧，有剧照 | 详情、选集、剧照 |
+| 漫画 | 详情、目录 |
+| 普通图集 | 详情、图集 |
+| 写真，无花絮 | 详情、写真 |
+| 写真，有花絮 | 详情、写真、拍摄花絮 |
+
+前端不得通过固定布尔配置强制展示空分区。分区可用性以服务端返回的已发布内容为准。
+
+### 6.4 内容关联
+
+系统应支持以下关联：
+
+- 某集视频关联对应剧照。
+- 某套写真关联对应拍摄花絮。
+- 海报或宣传图关联整个作品。
+- 漫画插图关联具体章节。
+
+关联只用于内容跳转和推荐，不将视频直接插入漫画阅读流。
+
+## 7. 信息架构
+
+### 7.1 Mini App 主导航
+
+MVP 建议包含：
+
+- 首页
+- 分类
+- 搜索
+- 收藏
+- 我的
+
+### 7.2 首页
+
+首页应支持：
+
+- 推荐作品。
+- 最近更新。
+- 按主类型展示的内容入口。
+- 继续阅读。
+- 最近请求播放的影视内容。
+
+首页不承担产品功能说明或营销落地页职责，打开 Mini App 后直接进入可使用的内容界面。
+
+### 7.3 分类与搜索
+
+支持：
+
+- 按作品主类型筛选。
+- 按标签、年份、地区或状态筛选；具体字段由内容规模决定。
+- 按关键词搜索作品名称和别名。
+- 搜索结果明确展示作品主类型和当前可用内容。
+- 空结果、加载失败和无权限结果应有独立状态。
+
+### 7.4 作品详情
+
+详情页包含：
+
+- 封面、名称、简介、标签和基础资料。
+- 收藏状态。
+- 根据已发布内容动态生成的分区。
+- 继续阅读或最近发送入口。
+- 影视选集、漫画目录、图集或写真列表。
+- 相关剧照、花絮或关联内容。
+
+主命令应由作品类型决定：
+
+- 影视：在 Telegram 播放。
+- 漫画：开始阅读或继续阅读。
+- 图集：查看图集。
+- 写真：查看写真。
+
+## 8. 图片浏览需求
+
+### 8.1 普通图集与写真
+
+采用图集浏览体验：
+
+- 先显示缩略图总览。
+- 点击后进入沉浸式查看。
+- 左右滑动切换图片。
+- 支持双指缩放、双击放大和拖动查看细节。
+- 点击画面显示或隐藏控制栏。
+- 显示当前位置和总图片数。
+- 支持上一套、下一套和返回详情。
+- 图片加载失败时允许单张重试。
+- 记录最后查看的图片位置。
+
+写真默认按图集处理。具有严格顺序的写真可由后台配置为连续阅读模式。
+
+### 8.2 漫画阅读器
+
+漫画图片必须有稳定页序，阅读器支持：
+
+- 纵向连续滚动。
+- 横向分页阅读。
+- 单页布局。
+- 双页布局；主要面向横屏和平板。
+- 从左到右、从右到左两种阅读顺序。
+- 双指缩放和双击放大。
+- 当前页、总页数和阅读进度。
+- 跳转指定页。
+- 上一章、下一章和返回目录。
+- 自动恢复上次章节和页码或滚动位置。
+- 临近页面预加载和远端页面资源释放。
+- 单页失败重试，不中断整个章节。
+
+阅读方式与页面布局是两个独立选项：
+
+- 阅读方式：连续滚动、分页阅读。
+- 页面布局：单页、双页。
+
+### 8.3 Telegram 手势适配
+
+- 进入阅读器后应展开 Mini App。
+- 正确处理 Telegram 返回按钮。
+- 缩放、翻页和 Telegram 收起手势发生冲突时，应优先保证阅读操作。
+- 退出阅读器后恢复 Mini App 常规手势行为。
+- 适配 Telegram 主题色、安全区域及动态视口高度。
+
+### 8.4 图片分享与下载
+
+- MVP 分享作品或内容单元的 Mini App 深链接，不分享 Bot Token 或 Telegram 临时文件地址。
+- 图片下载不作为 MVP 必达功能。
+- Mini App 图片无法实现 DRM 级防保存；系统通过短时签名地址、权限校验和防盗链降低直接访问风险。
+- 截图、录屏和设备层抓取不在可完全控制范围内。
+
+## 9. 影视分发与播放需求
+
+### 9.1 播放流程
+
+1. 用户在 Mini App 进入影视详情并选择具体内容。
+2. 用户点击“在 Telegram 播放”。
+3. 服务端验证 Telegram 身份、内容状态和访问权限。
+4. 系统创建发送任务，防止重复点击和超过 Bot 限流。
+5. Bot 从私有存储频道复制视频到用户私聊。
+6. 目标消息启用内容保护。
+7. Mini App 显示发送结果，引导用户返回聊天观看。
+8. 用户使用 Telegram 原生播放器观看。
+
+### 9.2 视频消息
+
+视频消息可包含：
+
+- 作品名、季和集数。
+- 上一集。
+- 下一集。
+- 重新选集。
+- 打开作品详情。
+- 收藏作品。
+
+每次只发送用户选择的当前内容，避免一次发送整部作品。
+
+### 9.3 内容保护
+
+- 所有发送或复制给用户的图片、视频和文件消息必须设置 `protect_content=true`。
+- 在 Telegram 官方客户端中，该设置用于限制转发和保存。
+- 已经以未保护状态发送的消息不能通过编辑补加保护，必须删除并重新发送。
+- 系统必须记录目标聊天 ID、目标消息 ID、发送时间和保护状态。
+- 存储频道必须保持私有，并限制管理员数量。
+
+### 9.4 内容保护边界
+
+产品不得对用户承诺严格 DRM。以下行为无法被完全阻止：
+
+- 屏幕录制或外部设备拍摄。
+- 修改版客户端或操作系统级抓取。
+- 用户在保护生效前获得的副本。
+
+`protect_content` 不等于自动过期。Bot API 对普通消息删除存在时间限制，因此会员到期后不能保证撤回较早发送的内容。
+
+### 9.5 播放历史
+
+Telegram 不向 Bot 提供原生播放器的真实播放事件，因此系统只能记录：
+
+- 用户请求了哪一集。
+- 视频是否发送成功。
+- 用户是否点击上一集、下一集或重新发送。
+
+系统不能记录：
+
+- 真实播放秒数。
+- 是否完整播放。
+- 倍速和暂停状态。
+- 精确续播位置。
+
+界面应使用“最近发送”或“最近选择”，不应将其描述为精确观看进度。
+
+### 9.6 视频文件要求
+
+为获得较好的 Telegram 原生播放体验，视频应优先满足：
+
+- MP4 容器。
+- H.264 视频编码。
+- AAC 音频编码。
+- 支持 fast-start。
+- 正确的时长、宽高和缩略图。
+- 作为 Telegram 视频消息存储，而不是普通文档。
+
+MVP 每个内容单元默认使用一个经过优化的播放版本。多清晰度、外挂字幕和多音轨不列入首版承诺。
+
+## 10. 收藏与历史
+
+### 10.1 收藏
+
+- 用户可收藏整个作品。
+- 内容级书签可作为后续能力。
+- 收藏列表按作品展示，不重复展示同一作品的各个媒体分区。
+
+### 10.2 阅读进度
+
+- 漫画记录章节、页码、阅读方式和布局偏好。
+- 图集记录最后查看的内容单元和图片位置。
+- 进度同时保存在本地和服务端，以服务端记录作为跨设备恢复依据。
+
+### 10.3 影视记录
+
+- 记录最近请求发送的影视内容。
+- 记录发送成功、失败和重复发送状态。
+- 不将发送成功等同于观看完成。
+
+## 11. 内容管理需求
+
+### 11.1 资源入库
+
+- 管理员直接将视频或图片上传到 Telegram 私有存储频道。
+- Bot 接收频道消息事件并记录媒体元数据。
+- 管理后台展示待编排资源。
+- 管理员将资源关联到作品、内容分区和内容单元。
+- 入库过程不要求 VPS 接收或永久保存完整媒体文件。
+
+### 11.2 作品管理
+
+- 创建和编辑作品基础资料。
+- 设置作品主类型和子类型。
+- 管理内容分区、内容单元和顺序。
+- 关联剧照、花絮、章节和图集。
+- 设置发布、下架和权限状态。
+- 预览当前用户实际可见的动态目录。
+
+### 11.3 用户与权限
+
+- 查询用户基本状态和最近活动。
+- 分配或撤销访问权限。
+- 设置作品级、分区级或内容级权限。
+- 查看视频发送记录和失败原因。
+- 管理员操作必须进入审计日志。
+
+## 12. 非功能需求
+
+### 12.1 性能
+
+- Mini App 首屏应优先加载文本、封面和首批内容，非首屏图片延迟加载。
+- 图集和漫画不得一次加载全部原图。
+- 视频发送请求应立即返回排队状态，实际发送由后台任务执行。
+- 重复点击同一内容不得创建大量重复发送任务。
+
+### 12.2 安全
+
+- 服务端必须验证 Telegram `initData` 签名和有效时间。
+- Bot Token、存储频道 ID 和数据库凭据不得出现在前端。
+- 图片地址使用短时签名和权限校验。
+- 管理后台使用 Telegram 管理员白名单和角色权限。
+- 所有媒体分发、权限变更和管理员操作保留审计记录。
+- 对搜索、发送、图片请求和管理接口实施速率限制。
+
+### 12.3 兼容性
+
+优先支持：
+
+- Telegram iOS 当前稳定版本。
+- Telegram Android 当前稳定版本。
+- Telegram Desktop 当前稳定版本。
+- 常见手机屏幕宽度和横竖屏切换。
+
+### 12.4 可用性与恢复
+
+- Bot 发送失败必须可重试并记录 Telegram 错误信息。
+- Telegram 文件路径失效时，图片代理应重新调用 `getFile`。
+- 数据库定期备份。
+- 服务重启后，未完成任务可继续处理或进入明确失败状态。
+
+## 13. MVP 范围
+
+### 13.1 首版包含
+
+- Telegram Mini App 身份验证。
+- 首页、分类、搜索和作品详情。
+- 动态内容分区。
+- 影视选集与受保护视频发送。
+- 普通图集和写真浏览器。
+- 漫画目录和阅读器。
+- 收藏、漫画进度和最近发送。
+- 私有频道资源入库。
+- 基础内容管理后台。
+- 用户权限和管理员审计。
+
+### 13.2 后续阶段
+
+- 多清晰度视频版本选择。
+- 更完整的会员、订单和订阅系统。
+- 内容推荐和排行榜策略。
+- 内容级书签和评论。
+- 图片下载或 Bot 文件包分发。
+- 更精细的资源失效与自动清理策略。
+- 独立网页或 CDN 播放层；仅在业务目标变化时评估。
+
+## 14. 验收标准
+
+### 14.1 动态目录
+
+- 无漫画内容的作品不展示漫画入口。
+- 无写真内容的作品不展示写真入口。
+- 无剧照的影视作品不展示剧照入口。
+- 有花絮的写真作品展示花絮入口，无花絮时隐藏。
+- 下架或无权访问的内容不影响目录展示结果。
+
+### 14.2 图片阅读
+
+- 普通图集可查看缩略图、全屏浏览、缩放和切换。
+- 漫画可切换滚动或分页，以及单页或双页布局。
+- 退出后再次进入可恢复最近章节和位置。
+- 单张图片失败可重试，其余页面仍可继续阅读。
+
+### 14.3 影视发送
+
+- 有权限用户可请求发送当前剧集。
+- 无权限用户收到明确提示且不创建发送任务。
+- 发送成功后用户可在 Telegram 原生播放器观看。
+- 目标视频消息限制转发和保存。
+- 系统保存目标消息 ID、发送时间和发送状态。
+- 重复快速点击不会无限重复发送。
+
+### 14.4 安全
+
+- 伪造或过期的 Telegram `initData` 被拒绝。
+- 前端网络请求和页面源代码中不出现 Bot Token。
+- 未签名或已过期的图片地址不可访问。
+- 普通用户不能访问管理接口。
+
+## 15. 待产品确认事项
+
+以下内容尚未在本轮确定，实施前需单独确认：
+
+1. 会员或权限的具体业务模式。
+2. 视频消息是否自动删除，以及采用何种时间策略。
+3. 是否默认启用单一 720p 视频版本。
+4. 排行榜和推荐内容的计算方式。
+5. 图片作品是否允许用户通过 Bot 获取原文件。
+6. 管理员入库时的审核和发布流程是否需要多人审批。
+7. 作品资料所需的地区、年份、演员、作者等完整字段集合。
+
--- /dev/null	2026-09-02 03:15:53
+++ docs/technical-development.md	2026-09-02 03:14:17
@@ -0,0 +1,833 @@
+# Telegram 影视 Mini App 技术开发文档
+
+## 1. 文档信息
+
+| 项目 | 内容 |
+|---|---|
+| 文档版本 | 1.0 |
+| 文档状态 | 技术基线 |
+| 更新日期 | 2026-09-02 |
+| 对应需求文档 | `docs/requirements.md` 1.0 |
+| 架构形态 | 模块化单体 + 独立 Bot Worker |
+
+## 2. 技术目标与约束
+
+### 2.1 技术目标
+
+1. 使用一套主要开发语言覆盖 Mini App、API、Bot 和管理后台。
+2. 视频不经过 VPS 存储和代理，由 Telegram 原生播放器承担播放。
+3. 图片按需从 Telegram 获取并流式转发，不永久保存于 VPS。
+4. 作品目录根据真实内容、发布状态和权限动态生成。
+5. 关键业务操作可追踪、可重试且具有幂等性。
+6. 以单台 VPS 完成 MVP 部署，并保留后续拆分能力。
+
+### 2.2 硬性约束
+
+- Mini App 必须通过 HTTPS 提供。
+- Bot Token 不得进入浏览器环境。
+- Telegram 云端 Bot API `getFile` 当前只支持下载不超过 20MB 的文件。
+- Telegram 文件下载地址包含 Bot Token，不能直接返回给 Mini App。
+- 面向用户发送的媒体消息必须在发送时启用内容保护。
+- Telegram 原生播放器不向业务系统提供真实播放进度。
+- Bot API 对普通消息删除存在时间窗口，不能把历史消息撤回作为长期权限保障。
+- 漫画内容不允许关联视频内容单元。
+
+## 3. 技术选型
+
+### 3.1 核心技术栈
+
+| 层级 | 技术 | 用途 |
+|---|---|---|
+| 开发语言 | TypeScript | Mini App、API、Bot Worker、管理后台 |
+| 运行时 | Node.js 当前 LTS | 服务端运行环境 |
+| 包管理 | pnpm workspace | Monorepo 依赖与脚本管理 |
+| Mini App | React + Vite | Telegram 内嵌前端应用 |
+| 管理后台 | React + Vite | 内容与权限管理 |
+| 路由 | React Router | Mini App 和后台路由 |
+| 服务端状态 | TanStack Query | 请求、缓存、重试和失效管理 |
+| 客户端状态 | Zustand | 阅读偏好和临时交互状态 |
+| 样式 | Tailwind CSS + CSS Variables | 响应式样式和 Telegram 主题适配 |
+| UI 原语 | Radix UI | 无样式基础交互组件 |
+| 图标 | Lucide Icons | 统一图标体系 |
+| 动画 | GSAP | 页面和控件的轻量过渡 |
+| 图集浏览 | PhotoSwipe | 缩放、滑动和沉浸式图片查看 |
+| API 框架 | NestJS + Fastify Adapter | REST API、鉴权、模块边界 |
+| Bot 框架 | grammY | Bot API、Webhook、按钮和消息分发 |
+| 数据库 | PostgreSQL | 核心业务数据 |
+| ORM | Drizzle ORM | 类型安全查询和数据库迁移 |
+| 缓存与队列 | Redis + BullMQ | 缓存、幂等锁和后台任务 |
+| 数据校验 | Zod | 前后端共享数据校验 |
+| API 文档 | OpenAPI | 接口契约和联调 |
+| 日志 | Pino | 结构化日志 |
+| 测试 | Vitest + Playwright | 单元、集成和端到端测试 |
+| 部署 | Docker Compose + Caddy | 容器编排、HTTPS 和反向代理 |
+
+### 3.2 选择 TypeScript 的原因
+
+- 前后端和 Bot 共用内容模型与接口契约。
+- grammY、React、NestJS 和 Telegram WebApp API 的生态成熟。
+- 当前视频不经过服务端，Node.js 的计算性能满足业务需求。
+- 降低同时维护 Python 和 TypeScript 两套业务模型的成本。
+
+Python 可用于未来的批量媒体检查或辅助脚本，但不作为核心在线服务语言。
+
+### 3.3 明确不采用的首版技术
+
+- Next.js 或服务端渲染：Mini App 不需要 SEO，Vite SPA 更简单。
+- MTProto 流媒体网关：已确定使用 Telegram 原生播放器。
+- HLS 视频服务：首版没有 Mini App 内嵌播放器。
+- 本地 Bot API Server：首版图片限制在云端 `getFile` 支持范围内，视频不下载。
+- 对象存储：首版媒体源使用 Telegram；数据库备份不受此限制。
+- Kubernetes：单台 VPS 的 MVP 不需要该复杂度。
+
+## 4. 总体架构
+
+```text
+                         Telegram
+                  +--------------------+
+                  | Mini App Container |
+                  | Bot API            |
+                  | Private Channel    |
+                  | Native Player      |
+                  +----------+---------+
+                             |
+                             |
++----------------------------+----------------------------+
+| VPS                                                      |
+|                                                          |
+|  Caddy                                                   |
+|    +-- Mini App static files                             |
+|    +-- Admin static files                                |
+|    +-- /api -> API                                       |
+|    +-- /webhook -> Bot Worker                            |
+|                                                          |
+|  API                    Bot Worker                       |
+|    +-- Auth               +-- Webhook                    |
+|    +-- Catalog            +-- Channel ingestion          |
+|    +-- Image proxy        +-- Protected copyMessage      |
+|    +-- Entitlement        +-- Inline callbacks           |
+|    +-- Admin              +-- Delivery retries           |
+|          |                       |                        |
+|          +----------+------------+                        |
+|                     |                                     |
+|              PostgreSQL + Redis                           |
++----------------------------------------------------------+
+```
+
+### 4.1 服务边界
+
+#### Mini App
+
+- Telegram 身份初始化。
+- 首页、分类、搜索和作品详情。
+- 动态内容分区。
+- 图集和漫画阅读。
+- 收藏、阅读进度和视频发送请求。
+
+#### API
+
+- 验证 Telegram `initData`。
+- 签发短时效业务会话。
+- 提供目录、作品、内容和权限接口。
+- 创建视频分发任务。
+- 流式代理 Telegram 图片。
+- 提供管理后台接口。
+
+#### Bot Worker
+
+- 接收 Telegram Webhook。
+- 处理私有频道的资源入库事件。
+- 消费视频分发任务。
+- 使用 `copyMessage` 或必要时使用 `sendVideo(file_id)`。
+- 处理上一集、下一集和重新选集按钮。
+- 处理可配置的消息清理任务。
+
+#### 管理后台
+
+- 作品、分区、内容单元和媒体关联管理。
+- 资源入库编排。
+- 发布状态和访问权限管理。
+- 用户、发送记录和审计查询。
+
+## 5. 代码仓库结构
+
+```text
+apps/
+  mini-app/
+  admin/
+  api/
+  bot-worker/
+
+packages/
+  contracts/
+  database/
+  telegram/
+  config/
+  ui/
+
+docs/
+  requirements.md
+  technical-development.md
+
+infra/
+  compose/
+  caddy/
+
+scripts/
+```
+
+### 5.1 共享包职责
+
+- `contracts`：Zod Schema、API 请求响应类型和错误码。
+- `database`：Drizzle Schema、迁移、查询帮助函数。
+- `telegram`：Bot API 客户端封装、消息模板和 Telegram 类型转换。
+- `config`：环境变量定义、加载与启动时校验。
+- `ui`：Mini App 和管理后台共享的基础 UI 原语。
+
+禁止前端引用包含 Bot Token、数据库代码或服务端 Telegram 客户端的包。
+
+## 6. 领域模型
+
+### 6.1 用户与权限
+
+#### users
+
+- 内部用户 ID。
+- Telegram User ID，唯一。
+- 用户名、显示名、语言和头像信息。
+- 用户状态：正常、冻结、删除。
+- 首次登录和最后活动时间。
+
+#### entitlements
+
+- 用户 ID。
+- 权限作用域：全局、作品、分区或内容单元。
+- 权限类型。
+- 生效时间和失效时间。
+- 来源和备注。
+
+#### admin_accounts / admin_roles
+
+- 管理员 Telegram User ID。
+- 角色和权限集合。
+- 状态和最后登录时间。
+
+### 6.2 内容模型
+
+#### works
+
+- 作品 ID。
+- 主类型：`video`、`comic`、`gallery`、`photoshoot`。
+- 子类型：电影、电视剧、短剧、普通图集等。
+- 名称、别名、简介、封面资源 ID。
+- 年份、地区、标签、作者或演员等扩展资料。
+- 状态：草稿、已发布、已下架。
+- 排序和发布时间。
+
+#### content_sections
+
+- 分区 ID 和作品 ID。
+- 分区类型：播放、选集、剧照、漫画目录、图集、写真、花絮。
+- 显示标题和排序。
+- 状态。
+
+分区表保存编排信息，但前端是否显示仍由分区内实际可见内容计算得出。
+
+#### content_units
+
+- 内容单元 ID 和分区 ID。
+- 类型：电影、剧集、短片、漫画章节、图片集、写真集、花絮视频。
+- 标题、序号、季号、集号或章节号。
+- 简介、封面和排序。
+- 发布状态和访问策略。
+
+#### media_assets
+
+- 媒体资源 ID。
+- 所属内容单元或作品。
+- 类型：视频、图片、缩略图、封面、文件。
+- 角色：正片、剧照、漫画页、写真页、花絮、海报等。
+- Telegram 存储频道 ID。
+- Telegram 源消息 ID。
+- `file_id` 和 `file_unique_id`。
+- 文件名、MIME、大小、宽高和时长。
+- 页序或资源顺序。
+- 状态：待编排、可用、失效、下架。
+
+`storage_chat_id + source_message_id` 是 Telegram 源消息的主要定位依据；`file_id` 用于 Bot API 文件复用或图片获取。
+
+#### content_relations
+
+- 来源作品或内容单元。
+- 目标作品或内容单元。
+- 关系类型：对应剧照、对应花絮、补充图片等。
+- 排序。
+
+### 6.3 用户行为
+
+#### favorites
+
+- 用户 ID 和作品 ID。
+- 收藏时间。
+- 用户和作品组合唯一。
+
+#### reading_progress
+
+- 用户 ID 和内容单元 ID。
+- 进度类型：漫画或图集。
+- 当前页、总页数或滚动锚点。
+- 阅读方式、页面布局和阅读方向。
+- 最近更新时间。
+
+#### media_deliveries
+
+- 分发请求 ID。
+- 用户、作品、内容单元和媒体资源 ID。
+- 源频道 ID 和源消息 ID。
+- 目标聊天 ID和目标消息 ID。
+- 状态：排队、发送中、成功、失败、已删除。
+- `protect_content` 状态。
+- 幂等键。
+- Telegram 错误码、错误描述和重试次数。
+- 创建、发送和删除时间。
+
+#### ingestion_items
+
+- Telegram 存储频道消息信息。
+- 媒体元数据。
+- 入库状态：待处理、已关联、忽略、失败。
+- 操作管理员和时间。
+
+#### admin_audit_logs
+
+- 管理员 ID。
+- 操作类型和目标对象。
+- 修改前后摘要。
+- 请求 ID、IP 和时间。
+
+### 6.4 业务约束
+
+- `video` 作品允许视频单元和剧照图集。
+- `comic` 作品只允许漫画章节和图片资源，不允许视频单元。
+- `gallery` 作品以普通图片集为主。
+- `photoshoot` 作品允许写真图集和花絮视频。
+- 已发布内容单元必须至少包含一个可用媒体资源。
+- 同一漫画章节内的页序必须唯一且连续性可检查。
+- 同一源频道消息不得被重复登记为不同的物理媒体来源，业务复用通过关联实现。
+
+跨表内容组合约束由领域服务在事务内验证；数据库使用唯一索引、外键和状态检查约束保证基础完整性。
+
+## 7. 动态目录算法
+
+服务端生成作品详情时：
+
+1. 检查作品已发布且用户可访问。
+2. 查询作品已配置分区。
+3. 按作品主类型过滤不允许的分区类型。
+4. 查询每个分区内已发布且用户可访问的内容单元。
+5. 过滤结果为空的分区。
+6. 按后台配置顺序返回最终分区及内容摘要。
+
+不得把 `has_comic`、`has_photoshoot` 等人工维护布尔值作为事实来源。可以建立缓存或物化统计字段，但必须可由真实内容重新计算。
+
+## 8. API 设计
+
+### 8.1 协议约定
+
+- 使用 HTTPS REST JSON API。
+- 通过 OpenAPI 生成接口说明和客户端类型。
+- 所有响应包含请求 ID。
+- 业务错误使用稳定错误码，不直接把 Telegram 或数据库内部异常暴露给前端。
+- 列表接口使用游标分页；管理后台小列表可使用页码分页。
+
+### 8.2 身份接口
+
+- `POST /auth/telegram`：验证 Mini App `initData` 并建立业务会话。
+- `POST /auth/refresh`：刷新短时效会话。
+- `POST /auth/logout`：注销当前会话。
+- `GET /me`：返回当前用户和权限摘要。
+
+### 8.3 内容接口
+
+- `GET /works`：作品列表、分类和筛选。
+- `GET /works/search`：关键词搜索。
+- `GET /works/{workId}`：作品详情和动态分区。
+- `GET /sections/{sectionId}/units`：内容单元列表。
+- `GET /units/{unitId}`：章节、剧集或图集详情。
+- `GET /units/{unitId}/images`：按序返回签名图片资源摘要。
+
+### 8.4 用户行为接口
+
+- `GET /favorites`：收藏列表。
+- `PUT /favorites/{workId}`：收藏作品。
+- `DELETE /favorites/{workId}`：取消收藏。
+- `PUT /reading-progress/{unitId}`：保存漫画或图集进度。
+- `GET /history`：阅读记录和最近发送。
+
+### 8.5 视频分发接口
+
+- `POST /deliveries/video`：为指定内容单元创建发送任务。
+- `GET /deliveries/{deliveryId}`：查询发送状态。
+
+创建分发任务时必须验证：
+
+- 用户状态正常。
+- 作品和内容单元已发布。
+- 当前用户具备访问权限。
+- 媒体资源状态可用。
+- 用户未超过并发或频率限制。
+- 相同幂等键未创建重复任务。
+
+### 8.6 图片媒体接口
+
+- `GET /media/images/{assetId}`：验证签名后流式返回图片。
+- `GET /media/thumbnails/{assetId}`：返回缩略图。
+
+签名至少绑定资源 ID、用户或权限摘要、失效时间和签名版本。日志必须隐藏完整签名参数。
+
+### 8.7 管理接口
+
+- 作品、分区、内容单元和媒体资源 CRUD。
+- 待入库资源列表和关联操作。
+- 发布、下架和排序。
+- 用户和权限管理。
+- 分发记录、任务和审计日志查询。
+
+管理接口统一位于 `/admin` 命名空间并实施角色权限控制。
+
+## 9. Telegram Mini App 集成
+
+### 9.1 初始化
+
+- 前端通过 Telegram WebApp JavaScript API 获取原始 `initData`。
+- 后端严格按照 Telegram 官方算法验证签名。
+- 校验 `auth_date`，默认只接受可配置的短时间窗口。
+- 可在 Redis 中短期记录已使用签名摘要，降低重放风险。
+- 验证成功后同步或创建用户，并签发短时效业务令牌。
+
+前端展示数据不得被用作身份事实，特别是 `initDataUnsafe.user` 不能替代后端签名验证。
+
+### 9.2 WebApp 能力封装
+
+在前端建立单一适配层，封装：
+
+- `ready` 和 `expand`。
+- BackButton。
+- 主题和颜色参数。
+- 安全区域与动态视口。
+- 垂直滑动控制。
+- 打开 Telegram 链接和关闭 Mini App。
+
+所有能力调用先进行版本和可用性检查，非 Telegram 浏览器环境使用开发模拟器。
+
+### 9.3 深链接
+
+作品和内容单元使用 `startapp` 参数生成 Mini App 深链接。服务端只接受短内部标识或签名参数，不在深链接中放置权限信息、Bot Token 或 Telegram 文件路径。
+
+## 10. Bot 集成
+
+### 10.1 Webhook
+
+- 生产环境使用 Webhook，开发环境可使用 Long Polling。
+- Webhook 使用不可猜测路径和 Telegram Secret Token Header。
+- 更新先快速确认，再将耗时任务提交至 BullMQ。
+- 更新 ID 用于去重，避免 Telegram 重试导致重复处理。
+
+### 10.2 存储频道入库
+
+1. Bot 作为管理员加入私有存储频道。
+2. 管理员使用 Telegram 客户端上传资源。
+3. Bot 接收 `channel_post` 更新。
+4. 提取频道 ID、消息 ID、媒体类型、`file_id`、`file_unique_id` 和文件元数据。
+5. 创建待编排入库记录。
+6. 管理员在后台将资源关联到内容单元。
+
+Bot 只能可靠处理加入后收到的更新。历史消息需要通过专用导入流程或管理员重新转发登记。
+
+### 10.3 视频分发
+
+默认使用 `copyMessage`：
+
+- 来源为私有存储频道和源消息 ID。
+- 目标为用户与 Bot 的私聊。
+- 设置 `protect_content=true`。
+- 可覆盖标题并附加内联按钮。
+- 保存返回的目标消息 ID。
+
+如果资源适合通过 `file_id` 复用，可使用 `sendVideo(file_id)`，但仍必须设置内容保护。大文件复制能力和目标客户端表现需要在 PoC 中按真实文件大小验证。
+
+### 10.4 消息按钮
+
+视频消息可提供：
+
+- 上一集和下一集回调。
+- 打开 Mini App 选集页。
+- 收藏或打开作品详情。
+
+回调只携带短内部标识。收到回调后重新检查权限，不信任旧按钮代表的历史权限。
+
+### 10.5 限流、幂等与重试
+
+- 分发幂等键建议由用户 ID、内容单元 ID 和短时间窗口组成。
+- 捕获 Telegram `429` 并遵循 `retry_after`。
+- 网络错误使用有上限的指数退避。
+- 权限、资源不存在和 Bot 被用户阻止等错误不进行无限重试。
+- 同一用户限制同时进行的发送任务数量。
+
+### 10.6 保护与删除
+
+- `protect_content` 必须在发送时设置，不能事后编辑补加。
+- 删除策略为可配置能力，不作为长期 DRM。
+- 普通消息删除受 Telegram 时间窗口限制。
+- 如果启用自动删除，删除任务必须在允许窗口内执行，并记录结果。
+- 用户权限到期后立即禁止新发送，但不能承诺撤回全部历史消息。
+
+## 11. 图片获取与代理
+
+### 11.1 入库约束
+
+- 单张需要在 Mini App 查看原图的图片不得超过云端 `getFile` 当前限制。
+- 漫画正文优先以 `document` 保存，避免 Telegram 照片压缩影响文字。
+- 同时保留较小预览图，用于列表和快速首屏。
+- 保存原始文件名和 MIME，因为 `getFile` 不保证完整保留这些信息。
+
+### 11.2 请求流程
+
+```text
+Mini App
+  -> 带短时签名的内部图片 URL
+  -> API 校验用户和资源权限
+  -> Redis 查询有效 file_path 缓存
+  -> 必要时调用 getFile
+  -> 服务端从 Telegram 读取并流式转发
+  -> 客户端显示
+```
+
+- Telegram 下载 URL 只在服务端使用。
+- `file_path` 缓存时间应短于 Telegram 保证有效期。
+- 数据采用流式转发，避免完整图片进入进程内存。
+- 客户端断开时中止上游请求。
+- 首版不在 VPS 永久缓存图片字节。
+
+### 11.3 HTTP 行为
+
+- 返回正确的 `Content-Type`、`Content-Length` 和缓存策略。
+- 权限相关图片默认采用私有缓存策略。
+- 不把上游 Telegram 错误页面作为图片返回。
+- 对超大、损坏或 MIME 不允许的文件返回稳定业务错误。
+- 限制每个用户的并发图片请求数量。
+
+### 11.4 图片保护边界
+
+Mini App 图片必须传输到浏览器才能显示，因此无法完全阻止截图、缓存或设备级抓取。技术措施只包括：
+
+- 短时签名。
+- 用户与内容权限校验。
+- 防盗链和速率限制。
+- 不暴露 Telegram 文件地址和 Bot Token。
+- 可选的用户级水印属于后续功能。
+
+## 12. Mini App 前端实现
+
+### 12.1 数据与状态
+
+- TanStack Query 管理作品、目录、图集、收藏和发送状态。
+- Zustand 仅管理阅读模式、控件显隐等短期客户端状态。
+- 阅读进度进行防抖保存，并在页面隐藏、退出阅读器和切换章节时立即提交。
+- API 契约从共享 Zod Schema 派生，禁止手工维护重复类型。
+
+### 12.2 普通图集
+
+- 缩略图网格使用响应式稳定尺寸，防止图片加载造成布局跳动。
+- PhotoSwipe 负责全屏、缩放、拖动和切换。
+- 只预取当前图片附近资源。
+- 原图加载失败时保留预览并提供重试。
+
+### 12.3 漫画阅读器
+
+- 使用浏览器原生滚动、IntersectionObserver 和图片懒加载。
+- 连续滚动模式不一次挂载全部高分辨率图片。
+- 分页模式维护明确页索引和缩放状态。
+- 放大状态下优先处理图片拖动，未放大时才处理翻页手势。
+- 双页模式仅在宽度允许时启用，窄屏自动回退单页并保留用户偏好。
+- 进度以稳定页索引为主，滚动锚点为辅。
+
+### 12.4 GSAP 使用边界
+
+GSAP 用于：
+
+- 页面进入和退出。
+- 阅读器控制栏显隐。
+- 目录展开、状态切换和轻量反馈。
+
+GSAP 不用于：
+
+- 接管原生长列表滚动。
+- 图片缩放核心逻辑。
+- 持续运行的大面积装饰动画。
+
+动画优先使用 `transform` 和 `opacity`，尊重 `prefers-reduced-motion`，并在 React 组件卸载时清理上下文。
+
+### 12.5 可访问性
+
+- 图标按钮提供可读标签和工具提示。
+- 控件具备足够点击区域。
+- 状态不只依赖颜色表达。
+- 图片提供合理替代文本；漫画页可使用章节和页码描述。
+- 动画可减弱或关闭。
+
+## 13. 后台任务设计
+
+### 13.1 队列
+
+建议至少建立：
+
+- `telegram-delivery`：媒体发送。
+- `telegram-delete`：可选的消息定时删除。
+- `media-reconcile`：资源元数据和失效状态检查。
+- `audit-export`：后续审计导出。
+
+### 13.2 任务要求
+
+- 每个任务具有唯一 ID 和幂等键。
+- 明确最大重试次数、退避策略和不可重试错误。
+- 任务状态与业务记录同步。
+- Worker 重启后未完成任务可恢复。
+- Redis 开启持久化或使用可靠托管实例，避免任务静默丢失。
+
+## 14. 安全设计
+
+### 14.1 密钥管理
+
+- Bot Token、数据库密码、Redis 密码和签名密钥通过环境变量或密钥文件注入。
+- 不写入仓库、前端包、日志和错误响应。
+- 支持密钥轮换和签名版本迁移。
+
+### 14.2 访问控制
+
+- 用户接口验证业务会话和资源权限。
+- 管理接口同时验证管理员身份和角色权限。
+- Bot 回调重新验证权限。
+- 存储频道只允许受控管理员和 Bot。
+
+### 14.3 接口防护
+
+- 对登录、搜索、图片和视频发送接口分别限流。
+- 限制请求体大小和允许的 Content-Type。
+- CORS 仅允许配置的 Mini App 和管理后台域名。
+- 使用安全响应头，管理后台禁止被任意站点嵌入。
+- 图片签名和 Telegram `initData` 摘要不得完整写入访问日志。
+
+### 14.4 审计
+
+记录：
+
+- 登录和鉴权失败。
+- 媒体发送请求及结果。
+- 权限授予和撤销。
+- 作品发布和下架。
+- 媒体关联和删除。
+- 管理员角色变化。
+
+## 15. 部署方案
+
+### 15.1 MVP 服务器
+
+建议初始配置：
+
+```text
+2 vCPU
+4 GB RAM
+40-80 GB SSD
+稳定的 HTTPS 网络
+```
+
+视频流量由 Telegram 承担。VPS 主要负载为 API、图片代理、数据库、Redis 和 Bot 消息任务。
+
+### 15.2 Docker Compose 服务
+
+- `caddy`
+- `api`
+- `bot-worker`
+- `postgres`
+- `redis`
+- Mini App 静态文件
+- 管理后台静态文件
+
+PostgreSQL 和 Redis 使用独立持久卷。VPS 不创建影视媒体持久卷。
+
+### 15.3 域名与路由
+
+建议拆分：
+
+- Mini App 域名。
+- 管理后台域名。
+- API 域名或 Mini App 同源 `/api`。
+- Telegram Webhook 使用独立受保护路径。
+
+Caddy 负责 TLS、压缩、安全响应头和反向代理。
+
+### 15.4 环境
+
+- `development`：Telegram API 模拟器或测试 Bot、Long Polling。
+- `staging`：独立测试 Bot、测试存储频道和测试数据库。
+- `production`：正式 Bot、正式私有频道和生产数据库。
+
+测试与生产不得共享 Bot Token、存储频道或数据库。
+
+## 16. 可观测性与备份
+
+### 16.1 日志
+
+所有服务使用结构化日志并包含：
+
+- 请求 ID。
+- 用户内部 ID；避免不必要记录完整个人资料。
+- 任务 ID和分发 ID。
+- Telegram 方法和脱敏错误信息。
+- 响应时间和结果状态。
+
+### 16.2 指标
+
+重点指标：
+
+- API 请求量、错误率和延迟。
+- 图片代理流量、错误率和上游耗时。
+- Bot 发送成功率、失败率和 `429` 数量。
+- BullMQ 队列长度、等待时间和失败任务。
+- PostgreSQL 连接、慢查询和存储增长。
+- Redis 内存和命中率。
+
+### 16.3 备份
+
+- PostgreSQL 每日自动备份。
+- 备份文件加密并存放在 VPS 之外。
+- 定期执行恢复演练。
+- Telegram 媒体不进入数据库备份，只备份定位信息和业务元数据。
+
+## 17. 测试策略
+
+### 17.1 单元测试
+
+- 作品类型与内容组合规则。
+- 动态目录过滤。
+- 权限计算。
+- Telegram `initData` 验证。
+- 分发幂等键和重试分类。
+- 图片签名验证。
+
+### 17.2 集成测试
+
+- PostgreSQL 事务和约束。
+- BullMQ 任务状态转换。
+- 模拟 Telegram Bot API 的发送、限流和失败。
+- `getFile` 失效后的刷新。
+- 管理操作审计。
+
+### 17.3 前端测试
+
+- 动态分区显示和隐藏。
+- 图集滑动、缩放和失败重试。
+- 漫画滚动、分页、单页和双页。
+- 阅读进度恢复。
+- Telegram 返回按钮和视口变化。
+- 视频发送的排队、成功和失败状态。
+
+### 17.4 真实 Telegram 验收
+
+使用测试 Bot 和测试频道，在 iOS、Android 和 Desktop 验证：
+
+- Mini App 启动和身份验证。
+- 私有频道资源入库。
+- 大小不同的视频 `copyMessage` 行为。
+- 受保护视频是否可播放。
+- 转发和保存入口是否被限制。
+- 上一集、下一集和打开 Mini App 按钮。
+- Bot 被用户阻止、资源删除和限流场景。
+
+## 18. 开发阶段
+
+### 阶段 0：技术验证
+
+- 创建测试 Bot 和私有频道。
+- 验证 `copyMessage + protect_content`。
+- 验证目标尺寸视频在各客户端播放。
+- 验证图片 `getFile` 和代理链路。
+
+### 阶段 1：工程基础
+
+- 建立 Monorepo、配置、数据库和基础部署。
+- 完成 Telegram 身份验证和用户模型。
+- 建立 Bot Webhook、队列和审计基础。
+
+### 阶段 2：内容与后台
+
+- 实现作品、分区、内容单元和媒体资源模型。
+- 实现频道入库和后台编排。
+- 实现动态目录和权限查询。
+
+### 阶段 3：Mini App 浏览与图片阅读
+
+- 首页、分类、搜索和详情。
+- 图集浏览器和漫画阅读器。
+- 收藏和阅读进度。
+
+### 阶段 4：影视分发
+
+- 视频发送任务。
+- 受保护复制、消息按钮和发送记录。
+- 限流、幂等、重试和可选删除策略。
+
+### 阶段 5：安全与上线
+
+- 管理权限、签名图片、速率限制和日志脱敏。
+- 跨客户端验收、性能测试和备份恢复演练。
+- Staging 灰度后发布生产。
+
+## 19. 主要风险与处理
+
+| 风险 | 影响 | 处理方式 |
+|---|---|---|
+| Telegram 服务或限流 | 视频发送延迟或失败 | BullMQ、幂等、`retry_after`、明确失败状态 |
+| `protect_content` 不是 DRM | 仍可能录屏或设备级复制 | 产品明确边界、限制转发保存、控制单次发送 |
+| 历史消息不能长期撤回 | 会员到期后仍可能播放 | 到期禁止新发送；删除仅作为短期策略 |
+| Bot 原生播放器无进度事件 | 无法精确续播 | 只展示最近发送，不伪装为精确进度 |
+| 图片超过 `getFile` 限制 | Mini App 无法获取原图 | 入库限制、生成较小浏览版本或后续本地 Bot API |
+| 图片代理流量增长 | VPS 带宽和连接压力 | 预览图、懒加载、并发限制，必要时引入 CDN |
+| 存储频道消息被删除 | 资源失效 | 管理权限最小化、定期一致性检查、失效告警 |
+| 大视频复制兼容性差异 | 某些客户端播放失败 | 上线前按真实编码、尺寸和客户端验证 |
+| 空分区或权限泄漏 | 用户看到无效入口 | 服务端动态目录统一计算，前端不自行推断 |
+
+## 20. 技术决策记录
+
+### ADR-001：全栈 TypeScript
+
+状态：接受。统一前后端和 Bot 开发语言，减少共享模型维护成本。
+
+### ADR-002：Telegram 原生视频播放
+
+状态：接受。放弃首版 Mini App 内嵌播放器、HLS 和 MTProto 网关，降低 VPS 存储与带宽成本。
+
+### ADR-003：Telegram 私有频道作为媒体来源
+
+状态：接受。视频通过消息复制分发；图片通过受控服务端代理读取。
+
+### ADR-004：动态内容分区
+
+状态：接受。目录从已发布且有权限的真实内容计算，不维护人工 `has_*` 事实字段。
+
+### ADR-005：模块化单体加独立 Worker
+
+状态：接受。API 与 Bot Worker 分进程部署，共享数据库和契约，满足 MVP 并保留拆分空间。
+
+## 21. 开发前待确认
+
+开始实现前需要冻结：
+
+1. 会员与访问权限规则。
+2. 视频消息自动删除策略。
+3. 测试视频的最大文件大小和默认编码规格。
+4. 图片浏览版本的最大文件大小和预览尺寸。
+5. 作品资料完整字段和管理后台角色。
+6. 正式域名、Bot、私有存储频道和测试环境资源。
+

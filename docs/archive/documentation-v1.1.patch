--- docs/requirements.md
+++ docs/requirements.md
@@ -4,8 +4,8 @@
 
 | 项目 | 内容 |
 |---|---|
-| 文档版本 | 1.0 |
-| 文档状态 | 需求基线 |
+| 文档版本 | 1.1 |
+| 文档状态 | 已确认需求基线 |
 | 更新日期 | 2026-09-02 |
 | 产品形态 | Telegram Bot + Telegram Mini App + 管理后台 |
 | 当前阶段 | MVP 规划 |
@@ -26,6 +26,8 @@
 4. 影视和图片原始资源主要存放在 Telegram，不永久存储在 VPS。
 5. 当前作品目录仅展示实际存在、已发布且用户有权访问的内容分区。
 6. 管理员可通过私有频道入库媒体，并在管理后台完成作品编排和发布。
+7. 通过会员权限开关控制会员内容；有效会员可访问全部已发布内容，普通用户不能访问会员内容。
+8. 排行榜和推荐根据用户对类型、标签、收藏、阅读和发送行为形成的偏好计算。
 
 ### 3.2 成功指标
 
@@ -36,6 +38,7 @@
 - Bot 视频发送成功率及平均发送耗时。
 - 图集首图加载时间、漫画翻页成功率和图片错误率。
 - 收藏、继续阅读和再次访问比例。
+- 个性化推荐点击率和推荐内容后的有效行为比例。
 - 被限流、资源失效和权限校验失败的比例。
 
 ### 3.3 非目标
@@ -51,7 +54,7 @@
 
 ## 4. 用户与角色
 
-### 4.1 普通用户
+### 4.1 所有终端用户
 
 - 通过 Telegram 身份进入 Mini App。
 - 浏览、搜索和收藏作品。
@@ -59,20 +62,28 @@
 - 请求 Bot 发送影视内容。
 - 查看最近阅读和最近发送记录。
 
-### 4.2 受限或会员用户
+### 4.2 会员用户
 
-- 具备指定作品、分区或内容单元的访问权限。
-- 权限到期后不能请求发送新的受限资源。
-- 已发送消息的可访问性受 Telegram 能力限制，详见内容保护边界。
+- 有效会员可浏览所有已发布文件和内容单元。
+- 会员失效后立即按普通用户权限处理。
+- 已发送消息不会因会员失效自动删除，其可访问性受 Telegram 能力限制。
 
-### 4.3 内容管理员
+### 4.3 普通用户
 
+- 可浏览所有公开内容。
+- 不能读取会员图片、漫画页或会员内容单元的媒体列表。
+- 不能请求 Bot 发送会员影视内容。
+- 可以看到用于说明内容存在的作品基础资料和会员标识，但不能获得受限文件。
+
+### 4.4 内容管理员
+
 - 创建和维护作品资料。
 - 将 Telegram 存储消息关联到作品、剧集、章节或图集。
 - 调整内容顺序、发布状态和访问权限。
 - 查看资源入库状态和发送失败记录。
+- 具备发布权限的单个管理员即可完成审核和发布，无需多人审批。
 
-### 4.4 系统管理员
+### 4.5 系统管理员
 
 - 管理管理员账号和角色。
 - 配置 Bot、存储频道、发送策略和资源保护策略。
@@ -90,6 +101,11 @@
 8. 影视作品可包含剧照或海报图集。
 9. 写真作品可包含拍摄花絮视频。
 10. 普通照片、写真和漫画使用不同的浏览逻辑，不强制共用同一种阅读体验。
+11. 会员权限通过全局开关和内容访问级别共同控制。
+12. 视频消息不进行自动删除。
+13. 同一内容存在多个已上传视频版本时，默认发送最高分辨率的可用版本。
+14. 图片作品不允许用户通过 Bot 获取源文件。
+15. 内容管理员采用单人审核发布流程，但所有操作必须审计。
 
 ## 6. 内容模型与组合规则
 
@@ -135,7 +151,7 @@
 | 写真，无花絮 | 详情、写真 |
 | 写真，有花絮 | 详情、写真、拍摄花絮 |
 
-前端不得通过固定布尔配置强制展示空分区。分区可用性以服务端返回的已发布内容为准。
+前端不得通过固定布尔配置强制展示空分区。分区可用性以服务端返回的已发布内容为准。会员权限开关启用时，普通用户无权访问的会员文件不得进入分区内容结果，也不得获得媒体地址。
 
 ### 6.4 内容关联
 
@@ -164,7 +180,8 @@
 
 首页应支持：
 
-- 推荐作品。
+- 根据当前用户偏好生成的推荐作品。
+- 根据当前用户偏好生成的个性化排行榜。
 - 最近更新。
 - 按主类型展示的内容入口。
 - 继续阅读。
@@ -200,6 +217,18 @@
 - 图集：查看图集。
 - 写真：查看写真。
 
+### 7.5 个性化推荐与排行榜
+
+推荐和排行榜使用当前用户的内容偏好计算。可使用的信号包括：
+
+- 作品主类型和标签偏好。
+- 收藏、取消收藏和重复访问。
+- 漫画与图集阅读记录。
+- 影视内容的请求发送记录；不能将发送成功视为观看完成。
+- 内容新鲜度和整体热度。
+
+新用户或行为数据不足时，使用最近更新、整体热度和用户主动选择的偏好作为冷启动结果。用户无权访问的会员文件不得进入可点击的推荐结果。
+
 ## 8. 图片浏览需求
 
 ### 8.1 普通图集与写真
@@ -215,6 +244,8 @@
 - 支持上一套、下一套和返回详情。
 - 图片加载失败时允许单张重试。
 - 记录最后查看的图片位置。
+- 图片浏览版本最大 10MB，并满足 Telegram 图片尺寸和宽高比限制。
+- 预览图根据客户端视口和设备像素比自适应选择。
 
 写真默认按图集处理。具有严格顺序的写真可由后台配置为连续阅读模式。
 
@@ -251,7 +282,8 @@
 ### 8.4 图片分享与下载
 
 - MVP 分享作品或内容单元的 Mini App 深链接，不分享 Bot Token 或 Telegram 临时文件地址。
-- 图片下载不作为 MVP 必达功能。
+- 不提供通过 Bot 获取图片源文件、图片文件包或原图消息的功能。
+- Mini App 不提供图片源文件下载命令。
 - Mini App 图片无法实现 DRM 级防保存；系统通过短时签名地址、权限校验和防盗链降低直接访问风险。
 - 截图、录屏和设备层抓取不在可完全控制范围内。
 
@@ -283,7 +315,7 @@
 
 ### 9.3 内容保护
 
-- 所有发送或复制给用户的图片、视频和文件消息必须设置 `protect_content=true`。
+- 所有面向用户发送或复制的视频和文件消息必须设置 `protect_content=true`；图片源文件不通过 Bot 分发。
 - 在 Telegram 官方客户端中，该设置用于限制转发和保存。
 - 已经以未保护状态发送的消息不能通过编辑补加保护，必须删除并重新发送。
 - 系统必须记录目标聊天 ID、目标消息 ID、发送时间和保护状态。
@@ -297,7 +329,7 @@
 - 修改版客户端或操作系统级抓取。
 - 用户在保护生效前获得的副本。
 
-`protect_content` 不等于自动过期。Bot API 对普通消息删除存在时间限制，因此会员到期后不能保证撤回较早发送的内容。
+`protect_content` 不等于自动过期。本项目不自动删除已发送的视频消息；会员到期后只停止新的会员内容访问和发送，历史消息继续保留。
 
 ### 9.5 播放历史
 
@@ -326,8 +358,9 @@
 - 支持 fast-start。
 - 正确的时长、宽高和缩略图。
 - 作为 Telegram 视频消息存储，而不是普通文档。
+- 上传文件最大目标为 Telegram Premium 用户单文件 4GB 上限。
 
-MVP 每个内容单元默认使用一个经过优化的播放版本。多清晰度、外挂字幕和多音轨不列入首版承诺。
+同一内容单元存在多个已上传视频版本时，系统按画面像素数选择最高分辨率的可用版本；像素数相同时优先选择管理员标记的主版本。视频不在 VPS 实时转码，所有上传版本必须预先符合 Telegram 兼容格式。用户手动切换清晰度、外挂字幕和多音轨不列入首版承诺。
 
 ## 10. 收藏与历史
 
@@ -367,15 +400,30 @@
 - 关联剧照、花絮、章节和图集。
 - 设置发布、下架和权限状态。
 - 预览当前用户实际可见的动态目录。
+- 具备发布权限的内容管理员可独立完成审核和发布，不建立多人会签流程。
 
 ### 11.3 用户与权限
 
 - 查询用户基本状态和最近活动。
-- 分配或撤销访问权限。
-- 设置作品级、分区级或内容级权限。
+- 设置全局会员权限开关。
+- 设置用户会员状态和有效期。
+- 设置作品级、分区级或内容级访问级别：公开或会员。
+- 会员权限开关关闭时，所有已发布内容按公开内容处理。
+- 会员权限开关开启时，有效会员可访问所有已发布内容，普通用户只能访问公开内容。
 - 查看视频发送记录和失败原因。
 - 管理员操作必须进入审计日志。
 
+### 11.4 作品资料字段
+
+作品资料字段均为非必填项，数据库保留空值，界面统一将空值显示为“未知”。字段集合包括：
+
+- 通用字段：原名、别名、简介、地区或国家、发行年份、发行日期、语言、题材或标签、状态、分级。
+- 影视字段：导演、演员、编剧、制片人、出品方或工作室、总集数、单集时长。
+- 漫画字段：作者、原作、作画、出版社、连载平台、连载状态。
+- 图集与写真字段：摄影师、模特或主体、工作室、拍摄日期、拍摄地点、辑数。
+
+作品主类型不适用的字段无需录入，但接口仍使用稳定字段结构返回默认显示值。
+
 ## 12. 非功能需求
 
 ### 12.1 性能
@@ -423,16 +471,17 @@
 - 收藏、漫画进度和最近发送。
 - 私有频道资源入库。
 - 基础内容管理后台。
-- 用户权限和管理员审计。
+- 会员权限开关、公开或会员内容级别和管理员审计。
+- 基于用户偏好的推荐和排行榜基础版本。
+- 多个已上传视频版本中的最高分辨率自动选择。
 
 ### 13.2 后续阶段
 
 - 多清晰度视频版本选择。
-- 更完整的会员、订单和订阅系统。
-- 内容推荐和排行榜策略。
+- 订单、支付和自动续费系统。
+- 推荐算法迭代和更复杂的相似用户模型。
 - 内容级书签和评论。
-- 图片下载或 Bot 文件包分发。
-- 更精细的资源失效与自动清理策略。
+- 更精细的资源失效检测策略。
 - 独立网页或 CDN 播放层；仅在业务目标变化时评估。
 
 ## 14. 验收标准
@@ -444,6 +493,8 @@
 - 无剧照的影视作品不展示剧照入口。
 - 有花絮的写真作品展示花絮入口，无花絮时隐藏。
 - 下架或无权访问的内容不影响目录展示结果。
+- 会员权限开启时，普通用户无法获得会员文件和媒体地址；有效会员可访问全部已发布内容。
+- 会员权限关闭时，已发布内容不再因会员级别被阻止。
 
 ### 14.2 图片阅读
 
@@ -451,6 +502,7 @@
 - 漫画可切换滚动或分页，以及单页或双页布局。
 - 退出后再次进入可恢复最近章节和位置。
 - 单张图片失败可重试，其余页面仍可继续阅读。
+- Mini App 和 Bot 均不存在向用户发送图片源文件的入口。
 
 ### 14.3 影视发送
 
@@ -460,6 +512,8 @@
 - 目标视频消息限制转发和保存。
 - 系统保存目标消息 ID、发送时间和发送状态。
 - 重复快速点击不会无限重复发送。
+- 多个可用视频版本存在时，默认发送最高分辨率版本。
+- 系统不创建视频消息自动删除任务，已发送消息保持在用户私聊中。
 
 ### 14.4 安全
 
@@ -468,15 +522,20 @@
 - 未签名或已过期的图片地址不可访问。
 - 普通用户不能访问管理接口。
 
-## 15. 待产品确认事项
+### 14.5 推荐、资料与发布
 
-以下内容尚未在本轮确定，实施前需单独确认：
+- 不同偏好的用户可以获得不同的推荐或排行榜顺序。
+- 冷启动用户可以获得基于热度和最近更新的结果。
+- 未填写的地区、年份、演员、作者等资料统一显示“未知”。
+- 一名具备发布权限的管理员可以独立完成入库审核和发布。
+- 所有单管理员发布操作均写入审计日志。
 
-1. 会员或权限的具体业务模式。
-2. 视频消息是否自动删除，以及采用何种时间策略。
-3. 是否默认启用单一 720p 视频版本。
-4. 排行榜和推荐内容的计算方式。
-5. 图片作品是否允许用户通过 Bot 获取原文件。
-6. 管理员入库时的审核和发布流程是否需要多人审批。
-7. 作品资料所需的地区、年份、演员、作者等完整字段集合。
+## 15. 已确认产品决策
 
+1. 设置全局会员权限开关。开关开启时，有效会员可访问全部已发布文件，普通用户不能访问标记为会员的文件；开关关闭时不执行会员内容限制。
+2. 视频消息不进行自动删除。
+3. 同一内容存在多个上传版本时，默认使用最高分辨率的可用视频。
+4. 排行榜和推荐内容根据用户主类型、标签、收藏、阅读和影视发送偏好计算；冷启动使用热度与最近更新。
+5. 图片作品不允许用户通过 Bot 获取源文件。
+6. 管理员入库、审核和发布无需多人审批，具备权限的单个管理员即可完成。
+7. 作品资料使用完整可选字段集合；地区、年份、演员、作者等均非必填，留空统一显示“未知”。
--- docs/technical-development.md
+++ docs/technical-development.md
@@ -4,10 +4,10 @@
 
 | 项目 | 内容 |
 |---|---|
-| 文档版本 | 1.0 |
-| 文档状态 | 技术基线 |
+| 文档版本 | 1.1 |
+| 文档状态 | 已确认技术基线 |
 | 更新日期 | 2026-09-02 |
-| 对应需求文档 | `docs/requirements.md` 1.0 |
+| 对应需求文档 | `docs/requirements.md` 1.1 |
 | 架构形态 | 模块化单体 + 独立 Bot Worker |
 
 ## 2. 技术目标与约束
@@ -20,6 +20,7 @@
 4. 作品目录根据真实内容、发布状态和权限动态生成。
 5. 关键业务操作可追踪、可重试且具有幂等性。
 6. 以单台 VPS 完成 MVP 部署，并保留后续拆分能力。
+7. 实施会员开关、公开或会员两级内容权限及个性化推荐。
 
 ### 2.2 硬性约束
 
@@ -28,9 +29,12 @@
 - Telegram 云端 Bot API `getFile` 当前只支持下载不超过 20MB 的文件。
 - Telegram 文件下载地址包含 Bot Token，不能直接返回给 Mini App。
 - 面向用户发送的媒体消息必须在发送时启用内容保护。
+- 视频消息不创建自动删除任务。
+- 多个视频版本存在时默认选择最高分辨率版本。
 - Telegram 原生播放器不向业务系统提供真实播放进度。
 - Bot API 对普通消息删除存在时间窗口，不能把历史消息撤回作为长期权限保障。
 - 漫画内容不允许关联视频内容单元。
+- 图片源文件不得通过 Bot 分发给用户。
 
 ## 3. 技术选型
 
@@ -140,7 +144,7 @@
 - 消费视频分发任务。
 - 使用 `copyMessage` 或必要时使用 `sendVideo(file_id)`。
 - 处理上一集、下一集和重新选集按钮。
-- 处理可配置的消息清理任务。
+- 不创建视频消息自动删除任务。
 
 #### 管理后台
 
@@ -148,6 +152,7 @@
 - 资源入库编排。
 - 发布状态和访问权限管理。
 - 用户、发送记录和审计查询。
+- 具备发布权限的单个管理员直接审核和发布内容。
 
 ## 5. 代码仓库结构
 
@@ -196,8 +201,16 @@
 - Telegram User ID，唯一。
 - 用户名、显示名、语言和头像信息。
 - 用户状态：正常、冻结、删除。
+- 会员状态和会员有效期。
 - 首次登录和最后活动时间。
 
+#### system_settings
+
+- 全局会员权限开关。
+- 推荐参数版本。
+- 当前测试或生产环境标识。
+- 修改管理员和修改时间。
+
 #### entitlements
 
 - 用户 ID。
@@ -206,6 +219,8 @@
 - 生效时间和失效时间。
 - 来源和备注。
 
+首版以会员状态作为默认全局权限：有效会员可访问全部已发布内容，普通用户只能访问 `public` 内容。细粒度授权表保留给特殊授权场景。
+
 #### admin_accounts / admin_roles
 
 - 管理员 Telegram User ID。
@@ -220,10 +235,15 @@
 - 主类型：`video`、`comic`、`gallery`、`photoshoot`。
 - 子类型：电影、电视剧、短剧、普通图集等。
 - 名称、别名、简介、封面资源 ID。
-- 年份、地区、标签、作者或演员等扩展资料。
+- 通用可选资料：原名、别名、简介、地区或国家、发行年份、发行日期、语言、标签、状态和分级。
+- 影视可选资料：导演、演员、编剧、制片人、出品方、总集数和时长。
+- 漫画可选资料：作者、原作、作画、出版社、连载平台和连载状态。
+- 图集或写真可选资料：摄影师、模特或主体、工作室、拍摄日期、地点和辑数。
 - 状态：草稿、已发布、已下架。
 - 排序和发布时间。
 
+所有资料字段在数据库中允许为 `NULL`。API 展示层将空值规范化为“未知”，不使用字符串“未知”污染原始数据。
+
 #### content_sections
 
 - 分区 ID 和作品 ID。
@@ -239,7 +259,7 @@
 - 类型：电影、剧集、短片、漫画章节、图片集、写真集、花絮视频。
 - 标题、序号、季号、集号或章节号。
 - 简介、封面和排序。
-- 发布状态和访问策略。
+- 发布状态和访问级别：`public` 或 `member`；未显式设置时继承作品或分区级别。
 
 #### media_assets
 
@@ -251,6 +271,7 @@
 - Telegram 源消息 ID。
 - `file_id` 和 `file_unique_id`。
 - 文件名、MIME、大小、宽高和时长。
+- 视频版本标识、像素数和管理员主版本标记。
 - 页序或资源顺序。
 - 状态：待编排、可用、失效、下架。
 
@@ -279,17 +300,32 @@
 - 阅读方式、页面布局和阅读方向。
 - 最近更新时间。
 
+#### content_events
+
+- 用户 ID、作品 ID和可选内容单元 ID。
+- 行为类型：展示、点击、收藏、取消收藏、阅读、重复访问、请求发送。
+- 行为权重、发生时间和推荐请求 ID。
+
+不记录 Telegram 原生播放器的虚构播放完成事件。
+
+#### user_interest_profiles
+
+- 用户 ID。
+- 主类型、标签和内容主题权重。
+- 最近更新和算法版本。
+- 冷启动偏好状态。
+
 #### media_deliveries
 
 - 分发请求 ID。
 - 用户、作品、内容单元和媒体资源 ID。
 - 源频道 ID 和源消息 ID。
 - 目标聊天 ID和目标消息 ID。
-- 状态：排队、发送中、成功、失败、已删除。
+- 状态：排队、发送中、成功、失败。
 - `protect_content` 状态。
 - 幂等键。
 - Telegram 错误码、错误描述和重试次数。
-- 创建、发送和删除时间。
+- 创建和发送时间。
 
 #### ingestion_items
 
@@ -311,6 +347,9 @@
 - `comic` 作品只允许漫画章节和图片资源，不允许视频单元。
 - `gallery` 作品以普通图片集为主。
 - `photoshoot` 作品允许写真图集和花絮视频。
+- 会员权限关闭时跳过内容会员级别检查；开启时普通用户只能访问 `public` 内容。
+- 图片源文件不建立面向用户的 Bot 分发操作。
+- 视频分发从可用版本中选择像素数最高的版本；像素数相同时选择管理员主版本。
 - 已发布内容单元必须至少包含一个可用媒体资源。
 - 同一漫画章节内的页序必须唯一且连续性可检查。
 - 同一源频道消息不得被重复登记为不同的物理媒体来源，业务复用通过关联实现。
@@ -322,14 +361,28 @@
 服务端生成作品详情时：
 
 1. 检查作品已发布且用户可访问。
-2. 查询作品已配置分区。
-3. 按作品主类型过滤不允许的分区类型。
-4. 查询每个分区内已发布且用户可访问的内容单元。
-5. 过滤结果为空的分区。
-6. 按后台配置顺序返回最终分区及内容摘要。
+2. 读取全局会员权限开关和当前用户会员状态。
+3. 查询作品已配置分区。
+4. 按作品主类型过滤不允许的分区类型。
+5. 查询每个分区内已发布且用户可访问的内容单元。
+6. 会员开关开启时过滤普通用户无权访问的 `member` 内容。
+7. 过滤结果为空的分区。
+8. 按后台配置顺序返回最终分区及内容摘要。
 
 不得把 `has_comic`、`has_photoshoot` 等人工维护布尔值作为事实来源。可以建立缓存或物化统计字段，但必须可由真实内容重新计算。
 
+### 7.1 个性化推荐与排行榜
+
+MVP 使用可解释的加权排序，不在首版引入离线机器学习平台。基础得分由以下部分组成：
+
+- 用户主类型和标签亲和度。
+- 收藏、阅读、重复访问和视频请求发送行为权重。
+- 内容新鲜度和全局热度。
+- 已经频繁展示或明确取消收藏的降权。
+- 当前用户权限过滤。
+
+`content_events` 通过后台任务聚合到 `user_interest_profiles`。推荐结果记录算法版本和推荐请求 ID，用于计算点击与后续行为。冷启动用户使用主动偏好、整体热度和最近更新。
+
 ## 8. API 设计
 
 ### 8.1 协议约定
@@ -352,6 +405,8 @@
 - `GET /works`：作品列表、分类和筛选。
 - `GET /works/search`：关键词搜索。
 - `GET /works/{workId}`：作品详情和动态分区。
+- `GET /recommendations`：当前用户个性化推荐。
+- `GET /rankings`：当前用户个性化排行榜。
 - `GET /sections/{sectionId}/units`：内容单元列表。
 - `GET /units/{unitId}`：章节、剧集或图集详情。
 - `GET /units/{unitId}/images`：按序返回签名图片资源摘要。
@@ -363,6 +418,7 @@
 - `DELETE /favorites/{workId}`：取消收藏。
 - `PUT /reading-progress/{unitId}`：保存漫画或图集进度。
 - `GET /history`：阅读记录和最近发送。
+- `POST /events/content`：记录允许的内容行为事件。
 
 ### 8.5 视频分发接口
 
@@ -375,6 +431,7 @@
 - 作品和内容单元已发布。
 - 当前用户具备访问权限。
 - 媒体资源状态可用。
+- 系统已经从可用版本中选择最高分辨率视频。
 - 用户未超过并发或频率限制。
 - 相同幂等键未创建重复任务。
 
@@ -390,7 +447,8 @@
 - 作品、分区、内容单元和媒体资源 CRUD。
 - 待入库资源列表和关联操作。
 - 发布、下架和排序。
-- 用户和权限管理。
+- 会员权限开关、用户会员状态和内容访问级别管理。
+- 单管理员审核和发布；发布动作必须写入审计日志。
 - 分发记录、任务和审计日志查询。
 
 管理接口统一位于 `/admin` 命名空间并实施角色权限控制。
@@ -441,11 +499,14 @@
 4. 提取频道 ID、消息 ID、媒体类型、`file_id`、`file_unique_id` 和文件元数据。
 5. 创建待编排入库记录。
 6. 管理员在后台将资源关联到内容单元。
+7. 具备发布权限的管理员独立检查并发布，不等待第二人审批。
 
 Bot 只能可靠处理加入后收到的更新。历史消息需要通过专用导入流程或管理员重新转发登记。
 
 ### 10.3 视频分发
 
+创建发送任务前，按 `width * height` 从当前内容单元的可用视频版本中选择最高分辨率；像素数相同时使用管理员标记的主版本。
+
 默认使用 `copyMessage`：
 
 - 来源为私有存储频道和源消息 ID。
@@ -454,8 +515,10 @@
 - 可覆盖标题并附加内联按钮。
 - 保存返回的目标消息 ID。
 
-如果资源适合通过 `file_id` 复用，可使用 `sendVideo(file_id)`，但仍必须设置内容保护。大文件复制能力和目标客户端表现需要在 PoC 中按真实文件大小验证。
+如果资源适合通过 `file_id` 复用，可使用 `sendVideo(file_id)`，但仍必须设置内容保护。正式目标包含由 Telegram Premium 用户上传到私有频道的最大 4GB 视频。4GB 是产品验收目标，不等于 Bot API 新上传接口的保证；阶段 0 必须以真实 4GB 文件验证 `copyMessage` 和 iOS、Android、Desktop 播放链路，未通过时视为平台阻塞项并记录实际可用上限。
 
+默认兼容规格为 MP4 容器、H.264/AVC 视频、AAC-LC 音频、`yuv420p` 像素格式和 fast-start。服务端不进行实时转码，不符合规格的资源在入库时标记为不可发布。
+
 ### 10.4 消息按钮
 
 视频消息可提供：
@@ -474,22 +537,24 @@
 - 权限、资源不存在和 Bot 被用户阻止等错误不进行无限重试。
 - 同一用户限制同时进行的发送任务数量。
 
-### 10.6 保护与删除
+### 10.6 保护与消息保留
 
 - `protect_content` 必须在发送时设置，不能事后编辑补加。
-- 删除策略为可配置能力，不作为长期 DRM。
-- 普通消息删除受 Telegram 时间窗口限制。
-- 如果启用自动删除，删除任务必须在允许窗口内执行，并记录结果。
-- 用户权限到期后立即禁止新发送，但不能承诺撤回全部历史消息。
+- 系统不创建或调度视频消息自动删除任务。
+- 用户权限到期后立即禁止新的会员内容访问和发送。
+- 已经发送成功的视频消息继续保留在用户私聊中。
+- `protect_content` 只限制官方客户端中的转发和保存，不作为 DRM 或到期撤回机制。
 
 ## 11. 图片获取与代理
 
 ### 11.1 入库约束
 
-- 单张需要在 Mini App 查看原图的图片不得超过云端 `getFile` 当前限制。
+- Mini App 图片浏览版本遵守 Bot API `sendPhoto` 当前上限：最大 10MB、宽高之和不超过 10000、宽高比不超过 20。
+- Telegram 图片源文件可以保留在私有频道，但超过云端 `getFile` 20MB 上限时必须另行生成符合上述限制的浏览版本。
 - 漫画正文优先以 `document` 保存，避免 Telegram 照片压缩影响文字。
-- 同时保留较小预览图，用于列表和快速首屏。
+- 同时保存 Telegram 返回的多个 `PhotoSize` 或配套预览图，用于列表和快速首屏。
 - 保存原始文件名和 MIME，因为 `getFile` 不保证完整保留这些信息。
+- 不为图片源文件创建面向用户的 Bot 发送或复制接口。
 
 ### 11.2 请求流程
 
@@ -516,6 +581,7 @@
 - 不把上游 Telegram 错误页面作为图片返回。
 - 对超大、损坏或 MIME 不允许的文件返回稳定业务错误。
 - 限制每个用户的并发图片请求数量。
+- 根据客户端视口宽度、设备像素比和当前用途选择最接近的预览尺寸；放大查看时再请求浏览版本。
 
 ### 11.4 图片保护边界
 
@@ -540,6 +606,7 @@
 
 - 缩略图网格使用响应式稳定尺寸，防止图片加载造成布局跳动。
 - PhotoSwipe 负责全屏、缩放、拖动和切换。
+- 根据视口宽度和设备像素比请求最接近的 Telegram `PhotoSize` 或配套预览图。
 - 只预取当前图片附近资源。
 - 原图加载失败时保留预览并提供重试。
 
@@ -583,8 +650,8 @@
 建议至少建立：
 
 - `telegram-delivery`：媒体发送。
-- `telegram-delete`：可选的消息定时删除。
 - `media-reconcile`：资源元数据和失效状态检查。
+- `interest-profile`：聚合用户偏好并更新推荐画像。
 - `audit-export`：后续审计导出。
 
 ### 13.2 任务要求
@@ -606,6 +673,7 @@
 ### 14.2 访问控制
 
 - 用户接口验证业务会话和资源权限。
+- 会员开关开启时，所有作品、分区、单元、图片签名和视频发送请求统一执行 `public/member` 权限判断。
 - 管理接口同时验证管理员身份和角色权限。
 - Bot 回调重新验证权限。
 - 存储频道只允许受控管理员和 Bot。
@@ -670,11 +738,13 @@
 ### 15.4 环境
 
 - `development`：Telegram API 模拟器或测试 Bot、Long Polling。
-- `staging`：独立测试 Bot、测试存储频道和测试数据库。
+- `staging`：独立测试 Bot、测试存储频道、测试域名、PostgreSQL 和 Redis。
 - `production`：正式 Bot、正式私有频道和生产数据库。
 
 测试与生产不得共享 Bot Token、存储频道或数据库。
 
+正式域名、Bot 和私有存储频道已由产品侧准备。最优测试环境使用独立 VPS，建议 `2 vCPU / 4GB RAM / 40GB SSD`，运行与生产相同的 Docker Compose 拓扑；使用独立 HTTPS 子域名、测试 Bot、测试频道、数据库卷和 Redis 卷。Staging 不接触生产用户或生产媒体记录。
+
 ## 16. 可观测性与备份
 
 ### 16.1 日志
@@ -711,7 +781,9 @@
 
 - 作品类型与内容组合规则。
 - 动态目录过滤。
-- 权限计算。
+- 会员开关、会员状态和内容访问级别计算。
+- 视频最高分辨率选择和同分辨率主版本规则。
+- 个性化推荐权重、冷启动和权限过滤。
 - Telegram `initData` 验证。
 - 分发幂等键和重试分类。
 - 图片签名验证。
@@ -723,6 +795,7 @@
 - 模拟 Telegram Bot API 的发送、限流和失败。
 - `getFile` 失效后的刷新。
 - 管理操作审计。
+- 单管理员审核发布流程。
 
 ### 17.3 前端测试
 
@@ -732,6 +805,8 @@
 - 阅读进度恢复。
 - Telegram 返回按钮和视口变化。
 - 视频发送的排队、成功和失败状态。
+- 普通用户和会员用户的内容可见性差异。
+- 客户端自适应预览尺寸选择。
 
 ### 17.4 真实 Telegram 验收
 
@@ -739,7 +814,9 @@
 
 - Mini App 启动和身份验证。
 - 私有频道资源入库。
-- 大小不同的视频 `copyMessage` 行为。
+- 包含 Telegram Premium 4GB 上限样本在内的不同尺寸视频 `copyMessage` 行为。
+- MP4/H.264/AAC-LC、`yuv420p` 和 fast-start 兼容性。
+- 10MB 边界图片、不同 `PhotoSize` 和高像素比客户端预览选择。
 - 受保护视频是否可播放。
 - 转发和保存入口是否被限制。
 - 上一集、下一集和打开 Mini App 按钮。
@@ -751,8 +828,9 @@
 
 - 创建测试 Bot 和私有频道。
 - 验证 `copyMessage + protect_content`。
-- 验证目标尺寸视频在各客户端播放。
+- 验证最高分辨率选择，并使用真实 4GB 视频在各客户端测试复制和播放。
 - 验证图片 `getFile` 和代理链路。
+- 验证图片浏览版本的 10MB、尺寸和宽高比边界。
 
 ### 阶段 1：工程基础
 
@@ -764,19 +842,20 @@
 
 - 实现作品、分区、内容单元和媒体资源模型。
 - 实现频道入库和后台编排。
-- 实现动态目录和权限查询。
+- 实现动态目录、会员开关、访问级别和单管理员发布。
 
 ### 阶段 3：Mini App 浏览与图片阅读
 
 - 首页、分类、搜索和详情。
 - 图集浏览器和漫画阅读器。
 - 收藏和阅读进度。
+- 个性化推荐、排行榜和冷启动结果。
 
 ### 阶段 4：影视分发
 
 - 视频发送任务。
 - 受保护复制、消息按钮和发送记录。
-- 限流、幂等、重试和可选删除策略。
+- 最高分辨率选择、限流、幂等和重试；不实现自动删除。
 
 ### 阶段 5：安全与上线
 
@@ -790,12 +869,12 @@
 |---|---|---|
 | Telegram 服务或限流 | 视频发送延迟或失败 | BullMQ、幂等、`retry_after`、明确失败状态 |
 | `protect_content` 不是 DRM | 仍可能录屏或设备级复制 | 产品明确边界、限制转发保存、控制单次发送 |
-| 历史消息不能长期撤回 | 会员到期后仍可能播放 | 到期禁止新发送；删除仅作为短期策略 |
+| 视频消息不自动删除 | 会员到期后历史消息仍可播放 | 作为已确认产品行为；到期只禁止新的访问和发送 |
 | Bot 原生播放器无进度事件 | 无法精确续播 | 只展示最近发送，不伪装为精确进度 |
-| 图片超过 `getFile` 限制 | Mini App 无法获取原图 | 入库限制、生成较小浏览版本或后续本地 Bot API |
+| 图片超过 Telegram 浏览边界 | Mini App 无法获取或显示 | 生成最大 10MB 且满足尺寸约束的浏览版本，源文件不向用户下发 |
 | 图片代理流量增长 | VPS 带宽和连接压力 | 预览图、懒加载、并发限制，必要时引入 CDN |
 | 存储频道消息被删除 | 资源失效 | 管理权限最小化、定期一致性检查、失效告警 |
-| 大视频复制兼容性差异 | 某些客户端播放失败 | 上线前按真实编码、尺寸和客户端验证 |
+| 4GB 视频复制兼容性差异 | Bot API 或某些客户端可能失败 | 阶段 0 使用真实 4GB 文件验收；未通过则记录平台实际上限并阻断发布 |
 | 空分区或权限泄漏 | 用户看到无效入口 | 服务端动态目录统一计算，前端不自行推断 |
 
 ## 20. 技术决策记录
@@ -820,14 +899,35 @@
 
 状态：接受。API 与 Bot Worker 分进程部署，共享数据库和契约，满足 MVP 并保留拆分空间。
 
-## 21. 开发前待确认
+### ADR-006：会员开关与两级访问权限
 
-开始实现前需要冻结：
+状态：接受。全局会员开关开启时，有效会员访问全部已发布内容，普通用户只能访问 `public` 内容；关闭时不执行会员内容限制。
 
-1. 会员与访问权限规则。
-2. 视频消息自动删除策略。
-3. 测试视频的最大文件大小和默认编码规格。
-4. 图片浏览版本的最大文件大小和预览尺寸。
-5. 作品资料完整字段和管理后台角色。
-6. 正式域名、Bot、私有存储频道和测试环境资源。
+### ADR-007：视频消息永久保留
 
+状态：接受。不创建自动删除任务；会员失效只阻止新的会员内容访问和发送。
+
+### ADR-008：最高分辨率视频与 4GB 验收目标
+
+状态：接受。从已上传可用版本中自动选择最高分辨率，使用 MP4/H.264/AAC-LC 兼容规格，并以 Telegram Premium 4GB 文件作为阶段 0 实测上限目标。
+
+### ADR-009：个性化推荐与排行榜
+
+状态：接受。首版使用基于用户类型、标签、收藏、阅读、视频发送、内容热度和新鲜度的可解释加权算法。
+
+### ADR-010：图片浏览边界与禁止源文件下发
+
+状态：接受。图片浏览版本遵守 Telegram `sendPhoto` 当前 10MB 及尺寸边界，预览尺寸按客户端自适应选择，不提供 Bot 图片源文件分发。
+
+### ADR-011：单管理员发布与可选作品资料
+
+状态：接受。具备发布权限的单个管理员可完成审核发布；作品扩展资料允许为空，展示层统一显示“未知”。
+
+## 21. 已确认开发基线
+
+1. 提供全局会员权限开关。开关开启时，有效会员可访问所有已发布文件，普通用户不能访问 `member` 文件；关闭时所有已发布内容按公开内容处理。
+2. 视频消息不进行自动删除，系统不创建 `telegram-delete` 队列或删除调度。
+3. 视频选择最高分辨率的已上传版本；目标最大测试文件为 Telegram Premium 4GB，默认规格为 MP4、H.264/AVC、AAC-LC、`yuv420p` 和 fast-start。
+4. 图片浏览版本采用 Telegram `sendPhoto` 当前最大 10MB、宽高之和不超过 10000、宽高比不超过 20 的边界；预览图按客户端视口和设备像素比自适应匹配。
+5. 作品资料字段采用需求文档第 11.4 节定义的完整可选集合，数据库空值由展示层显示为“未知”。
+6. 正式域名、正式 Bot 和正式私有存储频道已准备；测试环境采用独立 VPS、测试域名、测试 Bot、测试频道、PostgreSQL 和 Redis，不与生产共享凭据或数据。

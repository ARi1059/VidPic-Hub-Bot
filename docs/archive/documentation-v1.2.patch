--- docs/requirements.md
+++ docs/requirements.md
@@ -4,7 +4,7 @@
 
 | 项目 | 内容 |
 |---|---|
-| 文档版本 | 1.1 |
+| 文档版本 | 1.2 |
 | 文档状态 | 已确认需求基线 |
 | 更新日期 | 2026-09-02 |
 | 产品形态 | Telegram Bot + Telegram Mini App + 管理后台 |
@@ -28,6 +28,7 @@
 6. 管理员可通过私有频道入库媒体，并在管理后台完成作品编排和发布。
 7. 通过会员权限开关控制会员内容；有效会员可访问全部已发布内容，普通用户不能访问会员内容。
 8. 排行榜和推荐根据用户对类型、标签、收藏、阅读和发送行为形成的偏好计算。
+9. 会员作品对普通用户保留安全的基础资料和会员标识，用于引导购买会员，但绝不返回会员文件或媒体访问能力。
 
 ### 3.2 成功指标
 
@@ -41,6 +42,15 @@
 - 个性化推荐点击率和推荐内容后的有效行为比例。
 - 被限流、资源失效和权限校验失败的比例。
 
+MVP 上线验收使用以下基线：
+
+- 核心 API 在 50 RPS、持续 15 分钟的测试下，P95 响应时间不高于 500ms，服务端错误率低于 1%。
+- 视频发送请求在 500ms 内返回排队结果；排除用户未启动或屏蔽 Bot、源资源失效和 Telegram 平台故障后，发送任务成功率不低于 98%，P95 队列等待时间不高于 10 秒。
+- 图片代理在 10 个并发流、持续 15 分钟的测试下，P95 首字节时间不高于 1.5 秒，代理错误率低于 1%。
+- Mini App 首页在受支持客户端和稳定网络下，P75 首次可交互时间不高于 2.5 秒；图集首张预览图 P75 可见时间不高于 2 秒。
+- 月度业务服务可用性目标为 99.5%，不包含 Telegram 平台不可用时间。
+- PostgreSQL 业务数据 RPO 不高于 24 小时、RTO 不高于 4 小时，并至少每季度完成一次恢复演练。
+
 ### 3.3 非目标
 
 MVP 不包含：
@@ -73,7 +83,10 @@
 - 可浏览所有公开内容。
 - 不能读取会员图片、漫画页或会员内容单元的媒体列表。
 - 不能请求 Bot 发送会员影视内容。
-- 可以看到用于说明内容存在的作品基础资料和会员标识，但不能获得受限文件。
+- 可以在目录、搜索、推荐和排行榜中看到会员作品的安全基础资料、公开展示封面、会员标识和购买会员入口。
+- MVP 购买入口跳转到 Bot 内会员开通说明或人工服务流程；管理员确认后手动设置会员状态和有效期，不在 Mini App 内处理订单或支付。
+- 进入会员作品详情时，不返回会员分区、内容单元、资源数量、媒体地址、图片签名或视频发送能力。
+- 混合权限作品只返回公开分区和公开内容；界面可显示“包含会员内容”标识，但不泄露会员内容清单。
 
 ### 4.4 内容管理员
 
@@ -106,6 +119,9 @@
 13. 同一内容存在多个已上传视频版本时，默认发送最高分辨率的可用版本。
 14. 图片作品不允许用户通过 Bot 获取源文件。
 15. 内容管理员采用单人审核发布流程，但所有操作必须审计。
+16. 会员作品的安全基础资料可以用于会员转化；会员文件、目录明细和访问能力始终按权限过滤。
+17. 作品、分区和内容单元的权限使用逐级继承；子级显式设置覆盖父级，未设置时向上继承，作品未设置时默认为公开。
+18. Telegram Premium 4GB 视频的 `copyMessage + protect_content` 链路已在过往项目通过验证，当前项目复用该技术结论并在 Staging 执行冒烟回归。
 
 ## 6. 内容模型与组合规则
 
@@ -153,6 +169,15 @@
 
 前端不得通过固定布尔配置强制展示空分区。分区可用性以服务端返回的已发布内容为准。会员权限开关启用时，普通用户无权访问的会员文件不得进入分区内容结果，也不得获得媒体地址。
 
+会员锁定作品和动态分区使用不同的可见性规则：
+
+- 作品卡片和详情基础资料可以显示，用于呈现会员标识和购买会员入口。
+- 普通用户请求会员作品详情时，服务端返回 `access_granted=false` 和稳定的会员引导状态，不返回会员分区或内容单元列表。
+- 混合权限作品只生成当前用户有权访问的动态分区；会员内容存在性只以作品级标识表达，不返回受限目录明细。
+- 会员权限开关关闭时，所有已发布内容按公开内容处理，会员锁定状态和购买入口不显示。
+
+MVP 的 `membership_cta` 统一打开 Bot 内会员开通说明或人工服务流程，并携带短来源标识用于统计转化。自动订单、支付和续费不属于首版，会员由管理员确认后手动开通。
+
 ### 6.4 内容关联
 
 系统应支持以下关联：
@@ -227,7 +252,7 @@
 - 影视内容的请求发送记录；不能将发送成功视为观看完成。
 - 内容新鲜度和整体热度。
 
-新用户或行为数据不足时，使用最近更新、整体热度和用户主动选择的偏好作为冷启动结果。用户无权访问的会员文件不得进入可点击的推荐结果。
+新用户或行为数据不足时，使用最近更新、整体热度和用户主动选择的偏好作为冷启动结果。推荐和排行榜可以展示会员作品的锁定卡片用于会员转化，但点击后只能进入安全基础资料和会员购买入口；任何会员文件、目录明细或媒体地址均不得进入结果载荷。
 
 ## 8. 图片浏览需求
 
@@ -291,15 +316,19 @@
 
 ### 9.1 播放流程
 
-1. 用户在 Mini App 进入影视详情并选择具体内容。
-2. 用户点击“在 Telegram 播放”。
-3. 服务端验证 Telegram 身份、内容状态和访问权限。
-4. 系统创建发送任务，防止重复点击和超过 Bot 限流。
-5. Bot 从私有存储频道复制视频到用户私聊。
-6. 目标消息启用内容保护。
-7. Mini App 显示发送结果，引导用户返回聊天观看。
-8. 用户使用 Telegram 原生播放器观看。
+1. 用户首次进入时，系统确认其已经与 Bot 建立私聊且 Bot 当前可以向其发送消息。
+2. 未启动 Bot 的用户看到“打开 Bot”入口，通过带短参数的 `/start` 深链接建立会话后返回原作品；被用户屏蔽时显示解除屏蔽提示且不创建发送任务。
+3. 用户在 Mini App 进入影视详情并选择具体内容。
+4. 用户点击“在 Telegram 播放”。
+5. 服务端验证 Telegram 身份、内容状态、访问权限和 Bot 可发送状态。
+6. 系统创建发送任务，防止重复点击和超过 Bot 限流。
+7. Bot 从私有存储频道复制视频到用户私聊。
+8. 目标消息启用内容保护。
+9. Mini App 显示发送结果，引导用户返回聊天观看。
+10. 用户使用 Telegram 原生播放器观看。
 
+系统保存最近一次 Bot 可发送状态和检测时间，但每次实际发送失败仍以 Telegram 返回结果为准。用户完成 `/start` 或解除屏蔽后可以重试原请求，重试前必须重新执行权限校验。
+
 ### 9.2 视频消息
 
 视频消息可包含：
@@ -391,6 +420,9 @@
 - 管理后台展示待编排资源。
 - 管理员将资源关联到作品、内容分区和内容单元。
 - 入库过程不要求 VPS 接收或永久保存完整媒体文件。
+- 图片采用管理员本地预处理流程：源文件、最大 10MB 的浏览版本和必要缩略图分别上传到私有频道，并在后台关联为同一逻辑图片的不同版本。
+- 源文件超过 Bot API 云端 `getFile` 下载上限时，浏览版本必须在上传前生成；VPS 不负责下载该源文件后再转换。
+- 图片缺少合规浏览版本、MIME 不符合要求、尺寸越界或 Telegram 资源不可读取时，不允许发布对应内容单元。
 
 ### 11.2 作品管理
 
@@ -408,8 +440,10 @@
 - 设置全局会员权限开关。
 - 设置用户会员状态和有效期。
 - 设置作品级、分区级或内容级访问级别：公开或会员。
+- 子级显式访问级别覆盖父级；未设置时按内容单元、分区、作品的顺序向上继承，作品未设置时默认为公开。
 - 会员权限开关关闭时，所有已发布内容按公开内容处理。
 - 会员权限开关开启时，有效会员可访问所有已发布内容，普通用户只能访问公开内容。
+- 普通用户仍可看到会员作品安全基础资料和购买会员入口，但不会获得会员分区、内容单元或媒体访问能力。
 - 查看视频发送记录和失败原因。
 - 管理员操作必须进入审计日志。
 
@@ -417,12 +451,14 @@
 
 作品资料字段均为非必填项，数据库保留空值，界面统一将空值显示为“未知”。字段集合包括：
 
-- 通用字段：原名、别名、简介、地区或国家、发行年份、发行日期、语言、题材或标签、状态、分级。
+- 通用字段：原名、别名、简介、地区或国家、发行年份、发行日期、语言、题材或标签、发行状态、分级。
 - 影视字段：导演、演员、编剧、制片人、出品方或工作室、总集数、单集时长。
 - 漫画字段：作者、原作、作画、出版社、连载平台、连载状态。
 - 图集与写真字段：摄影师、模特或主体、工作室、拍摄日期、拍摄地点、辑数。
 
 作品主类型不适用的字段无需录入，但接口仍使用稳定字段结构返回默认显示值。
+
+“发行状态”表示连载中、已完结、已上映等作品业务资料；“发布状态”仅表示草稿、已发布或已下架，两者必须作为独立字段保存和展示。
 
 ## 12. 非功能需求
 
@@ -433,6 +469,8 @@
 - 视频发送请求应立即返回排队状态，实际发送由后台任务执行。
 - 重复点击同一内容不得创建大量重复发送任务。
 
+容量设计基线为：累计注册用户 10,000、日活用户 1,000、峰值在线用户 100、作品 5,000、内容单元 50,000、媒体元数据 500,000 条、核心 API 峰值 50 RPS、图片代理峰值 10 个并发流。该基线是工程验收负载，不是业务增长预测；容量超过任一基线的 70% 时触发扩容评估。
+
 ### 12.2 安全
 
 - 服务端必须验证 Telegram `initData` 签名和有效时间。
@@ -457,7 +495,15 @@
 - Telegram 文件路径失效时，图片代理应重新调用 `getFile`。
 - 数据库定期备份。
 - 服务重启后，未完成任务可继续处理或进入明确失败状态。
+- PostgreSQL 每日自动备份到 VPS 之外，满足 RPO 24 小时和 RTO 4 小时。
+- 关键指标超过成功指标基线时产生告警；上线前必须完成限流、资源失效、Bot 屏蔽和备份恢复演练。
 
+### 12.5 数据保留
+
+- 推荐原始行为事件保留 180 天，随后删除；聚合兴趣画像按用户最后活动时间滚动更新。
+- 应用运行日志默认保留 30 天，鉴权与安全日志保留 90 天，管理员审计日志保留 365 天。
+- 用户删除账号后，行为事件和兴趣画像在 30 天内删除或匿名化；依法或为安全审计必须保留的记录只保留最小必要字段。
+
 ## 13. MVP 范围
 
 ### 13.1 首版包含
@@ -492,7 +538,7 @@
 - 无写真内容的作品不展示写真入口。
 - 无剧照的影视作品不展示剧照入口。
 - 有花絮的写真作品展示花絮入口，无花絮时隐藏。
-- 下架或无权访问的内容不影响目录展示结果。
+- 下架内容不进入目录；无权访问的会员分区、内容单元和媒体不进入目录，会员作品只按锁定作品规则展示安全基础资料和购买入口。
 - 会员权限开启时，普通用户无法获得会员文件和媒体地址；有效会员可访问全部已发布内容。
 - 会员权限关闭时，已发布内容不再因会员级别被阻止。
 
@@ -506,6 +552,8 @@
 
 ### 14.3 影视发送
 
+- 未启动 Bot 的用户获得明确的启动入口且不创建发送任务；完成 `/start` 后可以返回并重试。
+- Bot 被用户屏蔽时记录稳定失败状态且不无限重试；解除屏蔽后可以重新提交。
 - 有权限用户可请求发送当前剧集。
 - 无权限用户收到明确提示且不创建发送任务。
 - 发送成功后用户可在 Telegram 原生播放器观看。
@@ -514,6 +562,7 @@
 - 重复快速点击不会无限重复发送。
 - 多个可用视频版本存在时，默认发送最高分辨率版本。
 - 系统不创建视频消息自动删除任务，已发送消息保持在用户私聊中。
+- 当前项目在 Staging 对既有 4GB 验证结论完成一次 `copyMessage + protect_content + 原生播放` 冒烟回归并记录结果；该回归不是重新评估技术可行性。
 
 ### 14.4 安全
 
@@ -539,3 +588,6 @@
 5. 图片作品不允许用户通过 Bot 获取源文件。
 6. 管理员入库、审核和发布无需多人审批，具备权限的单个管理员即可完成。
 7. 作品资料使用完整可选字段集合；地区、年份、演员、作者等均非必填，留空统一显示“未知”。
+8. 会员作品对普通用户保留安全基础资料、公开展示封面、会员标识和购买入口；会员分区、内容单元和媒体能力保持不可访问。
+9. Telegram Premium 4GB 视频链路已由过往项目验证通过，当前项目复用结论并只在 Staging 做冒烟回归。
+10. MVP 管理后台通过 Bot 的管理员专用 Web App 入口在 Telegram 内打开，使用 `initData`、管理员白名单和角色权限完成登录；首版不提供独立浏览器登录。
--- docs/technical-development.md
+++ docs/technical-development.md
@@ -4,10 +4,10 @@
 
 | 项目 | 内容 |
 |---|---|
-| 文档版本 | 1.1 |
+| 文档版本 | 1.2 |
 | 文档状态 | 已确认技术基线 |
 | 更新日期 | 2026-09-02 |
-| 对应需求文档 | `docs/requirements.md` 1.1 |
+| 对应需求文档 | `docs/requirements.md` 1.2 |
 | 架构形态 | 模块化单体 + 独立 Bot Worker |
 
 ## 2. 技术目标与约束
@@ -35,6 +35,7 @@
 - Bot API 对普通消息删除存在时间窗口，不能把历史消息撤回作为长期权限保障。
 - 漫画内容不允许关联视频内容单元。
 - 图片源文件不得通过 Bot 分发给用户。
+- Telegram Premium 4GB 视频的 `copyMessage + protect_content` 链路已在过往项目完成验证，当前项目将该结论作为已确认技术输入。
 
 ## 3. 技术选型
 
@@ -153,6 +154,7 @@
 - 发布状态和访问权限管理。
 - 用户、发送记录和审计查询。
 - 具备发布权限的单个管理员直接审核和发布内容。
+- 通过 Bot 向白名单管理员提供专用 Web App 入口，在 Telegram 内使用经过后端验证的 `initData` 登录；首版不支持独立浏览器登录。
 
 ## 5. 代码仓库结构
 
@@ -202,12 +204,14 @@
 - 用户名、显示名、语言和头像信息。
 - 用户状态：正常、冻结、删除。
 - 会员状态和会员有效期。
+- Bot 会话建立时间、最近可发送状态、状态检测时间和最近不可发送原因。
 - 首次登录和最后活动时间。
 
 #### system_settings
 
 - 全局会员权限开关。
 - 推荐参数版本。
+- 会员开通说明、人工服务入口和 `membership_cta` 版本。
 - 当前测试或生产环境标识。
 - 修改管理员和修改时间。
 
@@ -235,11 +239,13 @@
 - 主类型：`video`、`comic`、`gallery`、`photoshoot`。
 - 子类型：电影、电视剧、短剧、普通图集等。
 - 名称、别名、简介、封面资源 ID。
-- 通用可选资料：原名、别名、简介、地区或国家、发行年份、发行日期、语言、标签、状态和分级。
+- 通用可选资料：原名、别名、简介、地区或国家、发行年份、发行日期、语言、标签、发行状态和分级。
 - 影视可选资料：导演、演员、编剧、制片人、出品方、总集数和时长。
 - 漫画可选资料：作者、原作、作画、出版社、连载平台和连载状态。
 - 图集或写真可选资料：摄影师、模特或主体、工作室、拍摄日期、地点和辑数。
-- 状态：草稿、已发布、已下架。
+- 发布状态 `publication_status`：草稿、已发布、已下架。
+- 可选发行状态 `release_status`：未上映、连载中、已完结等业务资料值。
+- 可选访问级别 `access_level`：`public` 或 `member`；未设置时默认为 `public`。
 - 排序和发布时间。
 
 所有资料字段在数据库中允许为 `NULL`。API 展示层将空值规范化为“未知”，不使用字符串“未知”污染原始数据。
@@ -249,7 +255,8 @@
 - 分区 ID 和作品 ID。
 - 分区类型：播放、选集、剧照、漫画目录、图集、写真、花絮。
 - 显示标题和排序。
-- 状态。
+- 发布状态 `publication_status`。
+- 可选访问级别 `access_level`：`public` 或 `member`；未设置时继承作品。
 
 分区表保存编排信息，但前端是否显示仍由分区内实际可见内容计算得出。
 
@@ -259,7 +266,8 @@
 - 类型：电影、剧集、短片、漫画章节、图片集、写真集、花絮视频。
 - 标题、序号、季号、集号或章节号。
 - 简介、封面和排序。
-- 发布状态和访问级别：`public` 或 `member`；未显式设置时继承作品或分区级别。
+- 发布状态 `publication_status`。
+- 可选访问级别 `access_level`：`public` 或 `member`；未设置时先继承分区，再继承作品。
 
 #### media_assets
 
@@ -272,11 +280,14 @@
 - `file_id` 和 `file_unique_id`。
 - 文件名、MIME、大小、宽高和时长。
 - 视频版本标识、像素数和管理员主版本标记。
+- 图片逻辑资源 ID、父资源 ID和版本角色：`source`、`browse` 或 `thumbnail`。
 - 页序或资源顺序。
 - 状态：待编排、可用、失效、下架。
 
 `storage_chat_id + source_message_id` 是 Telegram 源消息的主要定位依据；`file_id` 用于 Bot API 文件复用或图片获取。
 
+同一逻辑图片可以对应多个 `media_assets`。`source` 只用于受控管理和归档；`browse` 是 Mini App 放大查看使用的合规版本；`thumbnail` 用于列表和首屏。会员锁定作品卡片使用的封面必须是单独标记为公开展示的资源，不能复用会员图片地址。
+
 #### content_relations
 
 - 来源作品或内容单元。
@@ -305,9 +316,13 @@
 - 用户 ID、作品 ID和可选内容单元 ID。
 - 行为类型：展示、点击、收藏、取消收藏、阅读、重复访问、请求发送。
 - 行为权重、发生时间和推荐请求 ID。
+- 事件来源：服务端业务事实或经过校验的客户端展示/点击。
+- 幂等键、算法版本和可选展示位。
 
 不记录 Telegram 原生播放器的虚构播放完成事件。
 
+收藏、取消收藏、阅读进度和请求发送由对应业务接口在服务端生成事件，客户端不能直接伪造。`POST /events/content` 只接受展示和点击事件，并要求推荐请求 ID、结果归属校验、幂等去重和独立速率限制。原始事件保留 180 天，聚合画像按用户状态更新；账号删除后 30 天内删除或匿名化相关行为数据。
+
 #### user_interest_profiles
 
 - 用户 ID。
@@ -348,6 +363,8 @@
 - `gallery` 作品以普通图片集为主。
 - `photoshoot` 作品允许写真图集和花絮视频。
 - 会员权限关闭时跳过内容会员级别检查；开启时普通用户只能访问 `public` 内容。
+- 有效访问级别按“内容单元显式值 -> 分区显式值 -> 作品显式值 -> `public` 默认值”计算，最接近资源的显式值优先。
+- 作品发现权限与媒体访问权限分离：普通用户可发现锁定会员作品，但只获得安全基础资料、公开展示封面、会员标识和购买入口。
 - 图片源文件不建立面向用户的 Bot 分发操作。
 - 视频分发从可用版本中选择像素数最高的版本；像素数相同时选择管理员主版本。
 - 已发布内容单元必须至少包含一个可用媒体资源。
@@ -360,14 +377,14 @@
 
 服务端生成作品详情时：
 
-1. 检查作品已发布且用户可访问。
+1. 检查作品发布状态；未发布或已下架作品对普通用户不可发现。
 2. 读取全局会员权限开关和当前用户会员状态。
-3. 查询作品已配置分区。
-4. 按作品主类型过滤不允许的分区类型。
-5. 查询每个分区内已发布且用户可访问的内容单元。
-6. 会员开关开启时过滤普通用户无权访问的 `member` 内容。
-7. 过滤结果为空的分区。
-8. 按后台配置顺序返回最终分区及内容摘要。
+3. 计算作品有效访问级别和 `access_granted`。
+4. 当普通用户访问锁定会员作品时，返回锁定作品 DTO：安全基础资料、公开展示封面、会员标识和购买入口；不查询或返回分区、内容单元、资源数量、媒体地址和分发能力。
+5. 对可访问作品查询已配置分区，并按作品主类型过滤不允许的分区类型。
+6. 逐项计算分区和内容单元有效访问级别；会员开关开启时过滤普通用户无权访问的 `member` 内容。
+7. 过滤结果为空的分区，按后台配置顺序返回最终分区及内容摘要。
+8. 会员权限开关关闭时跳过会员过滤，所有已发布内容视为可访问，并隐藏会员锁定状态和购买入口。
 
 不得把 `has_comic`、`has_photoshoot` 等人工维护布尔值作为事实来源。可以建立缓存或物化统计字段，但必须可由真实内容重新计算。
 
@@ -381,7 +398,7 @@
 - 已经频繁展示或明确取消收藏的降权。
 - 当前用户权限过滤。
 
-`content_events` 通过后台任务聚合到 `user_interest_profiles`。推荐结果记录算法版本和推荐请求 ID，用于计算点击与后续行为。冷启动用户使用主动偏好、整体热度和最近更新。
+`content_events` 通过后台任务聚合到 `user_interest_profiles`。推荐结果记录算法版本和推荐请求 ID，用于计算点击与后续行为。冷启动用户使用主动偏好、整体热度和最近更新。普通用户的推荐和排行榜可以包含锁定会员作品卡片，用于会员转化，但响应只携带锁定作品 DTO，绝不携带会员文件、目录明细或媒体访问能力。
 
 ## 8. API 设计
 
@@ -410,7 +427,11 @@
 - `GET /sections/{sectionId}/units`：内容单元列表。
 - `GET /units/{unitId}`：章节、剧集或图集详情。
 - `GET /units/{unitId}/images`：按序返回签名图片资源摘要。
+
+作品列表和详情统一返回 `access_granted`、`access_level` 和可选 `membership_cta`。锁定作品响应使用独立 Schema，并禁止出现 `sections`、`units`、资源数量、媒体 URL、图片签名和视频分发参数。混合权限作品只返回当前用户可访问的分区和单元。
 
+`membership_cta` 使用受控 Bot 深链接和短来源标识，打开会员开通说明或人工服务流程。MVP 不创建订单或支付接口；管理员确认完成后通过管理后台手动设置会员状态和有效期。
+
 ### 8.4 用户行为接口
 
 - `GET /favorites`：收藏列表。
@@ -418,7 +439,7 @@
 - `DELETE /favorites/{workId}`：取消收藏。
 - `PUT /reading-progress/{unitId}`：保存漫画或图集进度。
 - `GET /history`：阅读记录和最近发送。
-- `POST /events/content`：记录允许的内容行为事件。
+- `POST /events/content`：只记录经过推荐结果归属校验的展示和点击事件；收藏、阅读和发送行为由服务端业务接口生成。
 
 ### 8.5 视频分发接口
 
@@ -453,6 +474,14 @@
 
 管理接口统一位于 `/admin` 命名空间并实施角色权限控制。
 
+### 8.8 管理员身份
+
+- Bot 仅向 `admin_accounts` 白名单中的有效管理员返回专用 `web_app` 按钮。
+- 管理后台作为独立 React 应用部署，但 MVP 只允许在 Telegram Web App 容器内打开。
+- 管理前端提交原始 `initData` 到现有 `POST /auth/telegram`，后端完成签名、时效、管理员状态和角色校验后签发管理会话。
+- 管理会话使用独立受众、较短有效期和更严格的刷新策略，不能与普通用户令牌互换。
+- 直接在普通浏览器打开管理域名时不建立会话；首版不接入 Telegram Login Widget 或其他独立浏览器登录方案。
+
 ## 9. Telegram Mini App 集成
 
 ### 9.1 初始化
@@ -503,8 +532,16 @@
 
 Bot 只能可靠处理加入后收到的更新。历史消息需要通过专用导入流程或管理员重新转发登记。
 
-### 10.3 视频分发
+### 10.3 用户会话与可发送状态
 
+- Bot 在收到用户 `/start` 时记录 `bot_started_at` 和最近可发送状态；`/start` 参数只保存短内部跳转标识。
+- Mini App 创建视频任务前检查最近可发送状态。尚未启动 Bot 时返回稳定错误码和带原作品跳转参数的 Bot 深链接，不创建队列任务。
+- Telegram 返回 Bot 被屏蔽或目标聊天不可用时，更新用户可发送状态，任务直接失败且不进行无限重试。
+- 用户完成 `/start` 或解除屏蔽后可以重试；重试必须重新校验会员、内容发布状态和媒体可用性。
+- 缓存状态只用于提前提示，实际发送结果始终以 Telegram Bot API 返回为准。
+
+### 10.4 视频分发
+
 创建发送任务前，按 `width * height` 从当前内容单元的可用视频版本中选择最高分辨率；像素数相同时使用管理员标记的主版本。
 
 默认使用 `copyMessage`：
@@ -515,11 +552,11 @@
 - 可覆盖标题并附加内联按钮。
 - 保存返回的目标消息 ID。
 
-如果资源适合通过 `file_id` 复用，可使用 `sendVideo(file_id)`，但仍必须设置内容保护。正式目标包含由 Telegram Premium 用户上传到私有频道的最大 4GB 视频。4GB 是产品验收目标，不等于 Bot API 新上传接口的保证；阶段 0 必须以真实 4GB 文件验证 `copyMessage` 和 iOS、Android、Desktop 播放链路，未通过时视为平台阻塞项并记录实际可用上限。
+如果资源适合通过 `file_id` 复用，可使用 `sendVideo(file_id)`，但仍必须设置内容保护。Telegram Premium 4GB 视频的 `copyMessage + protect_content` 以及 iOS、Android、Desktop 原生播放链路已在过往项目验证通过，本项目将其作为已确认技术基线，不再列为可行性阻断项。Staging 上线前仍使用 4GB 样本执行一次冒烟回归，用于发现当前 Bot、频道或客户端环境配置回归；若失败，按环境缺陷处理并阻止该环境上线，不推翻既有技术结论。
 
 默认兼容规格为 MP4 容器、H.264/AVC 视频、AAC-LC 音频、`yuv420p` 像素格式和 fast-start。服务端不进行实时转码，不符合规格的资源在入库时标记为不可发布。
 
-### 10.4 消息按钮
+### 10.5 消息按钮
 
 视频消息可提供：
 
@@ -529,7 +566,7 @@
 
 回调只携带短内部标识。收到回调后重新检查权限，不信任旧按钮代表的历史权限。
 
-### 10.5 限流、幂等与重试
+### 10.6 限流、幂等与重试
 
 - 分发幂等键建议由用户 ID、内容单元 ID 和短时间窗口组成。
 - 捕获 Telegram `429` 并遵循 `retry_after`。
@@ -537,7 +574,7 @@
 - 权限、资源不存在和 Bot 被用户阻止等错误不进行无限重试。
 - 同一用户限制同时进行的发送任务数量。
 
-### 10.6 保护与消息保留
+### 10.7 保护与消息保留
 
 - `protect_content` 必须在发送时设置，不能事后编辑补加。
 - 系统不创建或调度视频消息自动删除任务。
@@ -550,14 +587,25 @@
 ### 11.1 入库约束
 
 - Mini App 图片浏览版本遵守 Bot API `sendPhoto` 当前上限：最大 10MB、宽高之和不超过 10000、宽高比不超过 20。
-- Telegram 图片源文件可以保留在私有频道，但超过云端 `getFile` 20MB 上限时必须另行生成符合上述限制的浏览版本。
+- Telegram 图片源文件可以保留在私有频道；管理员必须在本地预先生成符合上述限制的浏览版本和必要缩略图，再分别上传到私有频道。
+- 源文件超过云端 `getFile` 20MB 上限时，服务端永远不尝试下载源文件后转换，只读取管理员预先上传的浏览版本。
 - 漫画正文优先以 `document` 保存，避免 Telegram 照片压缩影响文字。
 - 同时保存 Telegram 返回的多个 `PhotoSize` 或配套预览图，用于列表和快速首屏。
 - 保存原始文件名和 MIME，因为 `getFile` 不保证完整保留这些信息。
 - 不为图片源文件创建面向用户的 Bot 发送或复制接口。
 
-### 11.2 请求流程
+### 11.2 图片版本编排与发布门槛
 
+1. 管理员在本地从源图生成最大 10MB、宽高之和不超过 10000、宽高比不超过 20 的 `browse` 版本，并根据列表用途生成 `thumbnail`。
+2. 管理员将 `source`、`browse` 和 `thumbnail` 分别上传到私有频道；Bot 为每条消息创建入库记录。
+3. 管理员在后台把这些记录关联到同一图片逻辑资源，设置版本角色、页序和对应作品或内容单元。
+4. 发布校验确认每一页至少有一个可由云端 `getFile` 获取的 `browse` 版本，MIME、大小和尺寸均符合约束；列表需要缩略图时同时校验 `thumbnail`。
+5. 校验失败的图片页或内容单元保持草稿状态并显示具体原因，不能绕过发布门槛。
+
+MVP 不在 VPS 上提供图片转码服务，不保存转换临时文件。未来若引入临时转换 Worker，必须另行评审资源隔离、临时文件销毁和大文件获取方案。
+
+### 11.3 请求流程
+
 ```text
 Mini App
   -> 带短时签名的内部图片 URL
@@ -574,7 +622,7 @@
 - 客户端断开时中止上游请求。
 - 首版不在 VPS 永久缓存图片字节。
 
-### 11.3 HTTP 行为
+### 11.4 HTTP 行为
 
 - 返回正确的 `Content-Type`、`Content-Length` 和缓存策略。
 - 权限相关图片默认采用私有缓存策略。
@@ -583,7 +631,7 @@
 - 限制每个用户的并发图片请求数量。
 - 根据客户端视口宽度、设备像素比和当前用途选择最接近的预览尺寸；放大查看时再请求浏览版本。
 
-### 11.4 图片保护边界
+### 11.5 图片保护边界
 
 Mini App 图片必须传输到浏览器才能显示，因此无法完全阻止截图、缓存或设备级抓取。技术措施只包括：
 
@@ -674,6 +722,7 @@
 
 - 用户接口验证业务会话和资源权限。
 - 会员开关开启时，所有作品、分区、单元、图片签名和视频发送请求统一执行 `public/member` 权限判断。
+- 对作品执行权限判断时同时返回 `access_granted`，不隐藏已发布会员作品的安全发现信息；对分区、单元、图片和视频分发则执行严格媒体访问过滤。
 - 管理接口同时验证管理员身份和角色权限。
 - Bot 回调重新验证权限。
 - 存储频道只允许受控管理员和 Bot。
@@ -707,11 +756,16 @@
 2 vCPU
 4 GB RAM
 40-80 GB SSD
-稳定的 HTTPS 网络
+100 Mbps 或更高网络端口
+每月不少于 1 TB 可用流量
 ```
 
 视频流量由 Telegram 承担。VPS 主要负载为 API、图片代理、数据库、Redis 和 Bot 消息任务。
 
+该规格对应以下容量基线：累计注册用户 10,000、日活用户 1,000、峰值在线用户 100、作品 5,000、内容单元 50,000、媒体元数据 500,000 条、核心 API 峰值 50 RPS、图片代理峰值 10 个并发流。该基线是工程验收负载，不是业务增长预测；容量达到任一基线的 70% 时执行扩容评估。
+
+上线前必须在同拓扑 Staging 完成 50 RPS 核心 API 和 10 个并发图片流各持续 15 分钟的测试。若 P95、错误率或资源水位不达标，生产配置升级至至少 `4 vCPU / 8GB RAM`，或拆分 PostgreSQL、Redis 和图片代理后重新测试，不能仅凭建议配置上线。
+
 ### 15.2 Docker Compose 服务
 
 - `caddy`
@@ -768,13 +822,29 @@
 - PostgreSQL 连接、慢查询和存储增长。
 - Redis 内存和命中率。
 
+MVP 告警与验收阈值：
+
+- 核心 API P95 不高于 500ms，5 分钟窗口服务端错误率低于 1%。
+- 视频请求接口在 500ms 内返回排队结果；排除用户未启动或屏蔽 Bot、资源失效和 Telegram 平台故障后，发送成功率不低于 98%，P95 队列等待不高于 10 秒。
+- 图片代理 P95 首字节时间不高于 1.5 秒，5 分钟窗口错误率低于 1%。
+- 负载测试期间 CPU 持续低于 75%，内存持续低于 80%，队列无持续增长。
+- 月度业务服务可用性目标为 99.5%，Telegram 平台中断单独记录。
+
 ### 16.3 备份
 
 - PostgreSQL 每日自动备份。
 - 备份文件加密并存放在 VPS 之外。
-- 定期执行恢复演练。
+- PostgreSQL RPO 为 24 小时、RTO 为 4 小时；至少每季度执行一次恢复演练。
+- Redis 开启 AOF 持久化并监控任务恢复；业务记录以 PostgreSQL 为准，队列恢复后执行任务状态对账。
 - Telegram 媒体不进入数据库备份，只备份定位信息和业务元数据。
 
+### 16.4 数据保留
+
+- 推荐原始行为事件保留 180 天，超过期限批量删除；聚合兴趣画像按用户最后活动时间持续更新。
+- 应用运行日志保留 30 天，鉴权与安全日志保留 90 天，管理员审计日志保留 365 天。
+- 用户账号删除后 30 天内删除或匿名化行为事件与兴趣画像；必须保留的安全审计记录只保留最小必要标识。
+- 保留任务由队列定期执行，执行数量、失败数量和删除数量进入监控。
+
 ## 17. 测试策略
 
 ### 17.1 单元测试
@@ -787,6 +857,9 @@
 - Telegram `initData` 验证。
 - 分发幂等键和重试分类。
 - 图片签名验证。
+- 作品、分区、内容单元三级权限继承和显式覆盖。
+- 锁定作品 DTO 不包含分区、单元、资源数量、媒体 URL 或分发参数。
+- 客户端事件归属校验、幂等去重和速率限制。
 
 ### 17.2 集成测试
 
@@ -796,6 +869,9 @@
 - `getFile` 失效后的刷新。
 - 管理操作审计。
 - 单管理员审核发布流程。
+- 管理员 Web App `initData`、白名单、角色和普通用户令牌隔离。
+- 未启动 Bot、已屏蔽、解除屏蔽后重试的视频分发状态。
+- 图片 `source`、`browse`、`thumbnail` 关联和发布门槛。
 
 ### 17.3 前端测试
 
@@ -806,6 +882,7 @@
 - Telegram 返回按钮和视口变化。
 - 视频发送的排队、成功和失败状态。
 - 普通用户和会员用户的内容可见性差异。
+- 普通用户可见会员作品锁定卡片和购买入口，但网络响应不包含会员目录或媒体能力。
 - 客户端自适应预览尺寸选择。
 
 ### 17.4 真实 Telegram 验收
@@ -814,7 +891,7 @@
 
 - Mini App 启动和身份验证。
 - 私有频道资源入库。
-- 包含 Telegram Premium 4GB 上限样本在内的不同尺寸视频 `copyMessage` 行为。
+- 使用 Telegram Premium 4GB 样本对既有 `copyMessage + protect_content` 验证结论执行当前环境冒烟回归。
 - MP4/H.264/AAC-LC、`yuv420p` 和 fast-start 兼容性。
 - 10MB 边界图片、不同 `PhotoSize` 和高像素比客户端预览选择。
 - 受保护视频是否可播放。
@@ -828,9 +905,10 @@
 
 - 创建测试 Bot 和私有频道。
 - 验证 `copyMessage + protect_content`。
-- 验证最高分辨率选择，并使用真实 4GB 视频在各客户端测试复制和播放。
+- 复用过往项目已经通过的 4GB 技术验证结论；在当前 Staging 使用真实 4GB 视频完成一次复制、内容保护和原生播放冒烟回归。
 - 验证图片 `getFile` 和代理链路。
 - 验证图片浏览版本的 10MB、尺寸和宽高比边界。
+- 验证 Bot `/start`、屏蔽、解除屏蔽和重新发送链路。
 
 ### 阶段 1：工程基础
 
@@ -843,6 +921,8 @@
 - 实现作品、分区、内容单元和媒体资源模型。
 - 实现频道入库和后台编排。
 - 实现动态目录、会员开关、访问级别和单管理员发布。
+- 实现锁定会员作品 DTO、公开展示封面和购买会员入口。
+- 实现图片多版本编排和发布校验。
 
 ### 阶段 3：Mini App 浏览与图片阅读
 
@@ -863,6 +943,14 @@
 - 跨客户端验收、性能测试和备份恢复演练。
 - Staging 灰度后发布生产。
 
+### 18.1 阶段准入条件
+
+- 阶段 1 状态为 `READY`，可以立即启动，4GB 技术可行性不再构成阻断项。
+- 阶段 2 所需的三级权限字段、继承算法、锁定作品 Schema 和 `publication_status` / `release_status` 命名已经在本文件冻结，状态为 `READY`。
+- 阶段 3 的图片多版本流程已经冻结，状态为 `READY`；开发完成后使用 `source`、`browse`、`thumbnail` 入库样本验收。
+- 阶段 4 的 Bot 会话和分发流程已经冻结，状态为 `READY`；开发完成后在 Staging 验收 `/start`、屏蔽恢复和 4GB 冒烟回归。
+- 阶段 5 上线前通过容量、SLO、备份恢复、数据保留和跨客户端验收。
+
 ## 19. 主要风险与处理
 
 | 风险 | 影响 | 处理方式 |
@@ -874,7 +962,7 @@
 | 图片超过 Telegram 浏览边界 | Mini App 无法获取或显示 | 生成最大 10MB 且满足尺寸约束的浏览版本，源文件不向用户下发 |
 | 图片代理流量增长 | VPS 带宽和连接压力 | 预览图、懒加载、并发限制，必要时引入 CDN |
 | 存储频道消息被删除 | 资源失效 | 管理权限最小化、定期一致性检查、失效告警 |
-| 4GB 视频复制兼容性差异 | Bot API 或某些客户端可能失败 | 阶段 0 使用真实 4GB 文件验收；未通过则记录平台实际上限并阻断发布 |
+| 4GB 当前环境配置回归 | 已验证链路可能因 Bot、频道或客户端环境变化失败 | 复用过往验证结论，Staging 使用真实 4GB 样本冒烟；失败按环境缺陷处理并阻断该环境上线 |
 | 空分区或权限泄漏 | 用户看到无效入口 | 服务端动态目录统一计算，前端不自行推断 |
 
 ## 20. 技术决策记录
@@ -907,9 +995,9 @@
 
 状态：接受。不创建自动删除任务；会员失效只阻止新的会员内容访问和发送。
 
-### ADR-008：最高分辨率视频与 4GB 验收目标
+### ADR-008：最高分辨率视频与已验证 4GB 链路
 
-状态：接受。从已上传可用版本中自动选择最高分辨率，使用 MP4/H.264/AAC-LC 兼容规格，并以 Telegram Premium 4GB 文件作为阶段 0 实测上限目标。
+状态：接受。从已上传可用版本中自动选择最高分辨率，使用 MP4/H.264/AAC-LC 兼容规格。Telegram Premium 4GB 文件的复制、内容保护和原生播放链路已在过往项目验证通过，本项目复用结论并在 Staging 做环境冒烟回归。
 
 ### ADR-009：个性化推荐与排行榜
 
@@ -923,6 +1011,22 @@
 
 状态：接受。具备发布权限的单个管理员可完成审核发布；作品扩展资料允许为空，展示层统一显示“未知”。
 
+### ADR-012：会员作品可发现但媒体隔离
+
+状态：接受。普通用户可以在目录、搜索、推荐和排行榜中看到会员作品的安全基础资料、公开展示封面、会员标识和购买入口；服务端不返回会员分区、内容单元、媒体地址或分发能力。
+
+### ADR-013：三级权限继承
+
+状态：接受。`works`、`content_sections` 和 `content_units` 均支持可选 `access_level`；有效值按内容单元、分区、作品、公开默认值的顺序解析，子级显式值覆盖父级。
+
+### ADR-014：管理员 Telegram Web App 登录
+
+状态：接受。MVP 管理后台通过 Bot 的管理员专用 Web App 入口在 Telegram 内打开，使用已验证 `initData`、管理员白名单和角色权限登录；不提供独立浏览器登录。
+
+### ADR-015：管理员本地生成图片浏览版本
+
+状态：接受。管理员在上传前生成 `browse` 和 `thumbnail` 版本，分别存入 Telegram 并与 `source` 关联；VPS 不承担图片转码或永久存储。
+
 ## 21. 已确认开发基线
 
 1. 提供全局会员权限开关。开关开启时，有效会员可访问所有已发布文件，普通用户不能访问 `member` 文件；关闭时所有已发布内容按公开内容处理。
@@ -931,3 +1035,16 @@
 4. 图片浏览版本采用 Telegram `sendPhoto` 当前最大 10MB、宽高之和不超过 10000、宽高比不超过 20 的边界；预览图按客户端视口和设备像素比自适应匹配。
 5. 作品资料字段采用需求文档第 11.4 节定义的完整可选集合，数据库空值由展示层显示为“未知”。
 6. 正式域名、正式 Bot 和正式私有存储频道已准备；测试环境采用独立 VPS、测试域名、测试 Bot、测试频道、PostgreSQL 和 Redis，不与生产共享凭据或数据。
+7. 普通用户可以发现会员作品并看到会员标识和购买入口，但不能获得会员分区、内容单元、媒体地址和分发能力。
+8. 三级权限字段及继承顺序已经冻结：内容单元显式值优先，其次分区、作品，最终默认为 `public`。
+9. 4GB 视频链路已由过往项目验证通过，当前项目只需在 Staging 完成环境冒烟回归。
+10. 管理后台通过 Telegram 内管理员专用 Web App 登录；管理员本地生成图片浏览版本和缩略图，VPS 不转码。
+11. MVP 容量、SLO、RPO、RTO、日志与行为事件保留期以第 15、16 节为验收基线。
+12. 锁定作品的会员入口跳转 Bot 内开通说明或人工服务流程，管理员手动开通会员；MVP 不实现订单和支付。
+
+## 22. 开发就绪结论
+
+- 产品范围、技术路线、三级权限、会员转化、Bot 会话、图片多版本、管理员登录、事件治理和非功能指标均已冻结。
+- Telegram Premium 4GB 视频链路采用过往项目已通过的验证结论，不存在待确认的技术可行性阻断。
+- 阶段 1 至阶段 4 均已具备开发输入，可以按第 18 节顺序实施；各阶段完成后执行对应测试，不需要再次进行产品决策。
+- 当前项目开发状态为 `GO`。生产上线仍必须通过 Staging、容量、安全、备份恢复和跨客户端验收，这些属于发布质量门槛，不影响开始开发。


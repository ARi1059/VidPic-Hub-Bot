--- docs/requirements.md
+++ docs/requirements.md
@@ -4,7 +4,7 @@
 
 | 项目 | 内容 |
 |---|---|
-| 文档版本 | 1.2 |
+| 文档版本 | 1.3 |
 | 文档状态 | 已确认需求基线 |
 | 更新日期 | 2026-09-02 |
 | 产品形态 | Telegram Bot + Telegram Mini App + 管理后台 |
@@ -40,6 +40,7 @@
 - 图集首图加载时间、漫画翻页成功率和图片错误率。
 - 收藏、继续阅读和再次访问比例。
 - 个性化推荐点击率和推荐内容后的有效行为比例。
+- 会员作品入口打开率，以及入口打开后 30 天内的会员激活转化率。
 - 被限流、资源失效和权限校验失败的比例。
 
 MVP 上线验收使用以下基线：
@@ -85,7 +86,7 @@
 - 不能请求 Bot 发送会员影视内容。
 - 可以在目录、搜索、推荐和排行榜中看到会员作品的安全基础资料、公开展示封面、会员标识和购买会员入口。
 - MVP 购买入口跳转到 Bot 内会员开通说明或人工服务流程；管理员确认后手动设置会员状态和有效期，不在 Mini App 内处理订单或支付。
-- 进入会员作品详情时，不返回会员分区、内容单元、资源数量、媒体地址、图片签名或视频发送能力。
+- 进入完全锁定的会员作品详情时，不返回会员分区、内容单元、资源数量、媒体地址、图片签名或视频发送能力。
 - 混合权限作品只返回公开分区和公开内容；界面可显示“包含会员内容”标识，但不泄露会员内容清单。
 
 ### 4.4 内容管理员
@@ -122,6 +123,9 @@
 16. 会员作品的安全基础资料可以用于会员转化；会员文件、目录明细和访问能力始终按权限过滤。
 17. 作品、分区和内容单元的权限使用逐级继承；子级显式设置覆盖父级，未设置时向上继承，作品未设置时默认为公开。
 18. Telegram Premium 4GB 视频的 `copyMessage + protect_content` 链路已在过往项目通过验证，当前项目复用该技术结论并在 Staging 执行冒烟回归。
+19. 作品权限响应使用 `full`、`partial`、`locked` 三态；必须先计算已发布子级的有效权限，再决定作品是否完全锁定。
+20. 所有用户可见作品封面使用独立公开展示资源，不复用会员正文、图片源文件或受保护媒体。
+21. 会员引导打开和会员激活由服务端记录，并使用最近 30 天有效入口完成转化归因。
 
 ## 6. 内容模型与组合规则
 
@@ -172,11 +176,14 @@
 会员锁定作品和动态分区使用不同的可见性规则：
 
 - 作品卡片和详情基础资料可以显示，用于呈现会员标识和购买会员入口。
-- 普通用户请求会员作品详情时，服务端返回 `access_granted=false` 和稳定的会员引导状态，不返回会员分区或内容单元列表。
-- 混合权限作品只生成当前用户有权访问的动态分区；会员内容存在性只以作品级标识表达，不返回受限目录明细。
+- 服务端先计算全部已发布分区和内容单元的有效权限，再返回 `access_state=full|partial|locked`。
+- `full`：当前用户可以访问作品内全部已发布内容；返回完整动态分区。
+- `partial`：至少存在一个公开内容单元，同时还存在不可访问的会员内容；只返回公开分区和公开内容，并显示“包含会员内容”标识及购买入口。
+- `locked`：当前用户没有任何可访问内容单元；只返回安全基础资料、公开展示封面、会员标识和购买入口，不返回分区、内容单元、资源数量或媒体能力。
+- 作品为 `member`、部分子级显式为 `public` 且仍存在 `member` 子级时，子级覆盖规则生效，普通用户获得 `partial` 响应和公开子级；如果全部已发布子级均显式为 `public`，则返回 `full`。不得因作品级会员标记提前返回 `locked`。
 - 会员权限开关关闭时，所有已发布内容按公开内容处理，会员锁定状态和购买入口不显示。
 
-MVP 的 `membership_cta` 统一打开 Bot 内会员开通说明或人工服务流程，并携带短来源标识用于统计转化。自动订单、支付和续费不属于首版，会员由管理员确认后手动开通。
+MVP 的 `membership_cta` 统一打开 Bot 内会员开通说明或人工服务流程，并携带绑定当前用户的短时来源标识。Bot 成功处理入口时由服务端记录 `membership_cta_open`；管理员将用户从非会员调整为有效会员时记录 `membership_activated`，并归因到该用户最近 30 天内最后一次有效入口。自动订单、支付和续费不属于首版，会员由管理员确认后手动开通。
 
 ### 6.4 内容关联
 
@@ -423,6 +430,7 @@
 - 图片采用管理员本地预处理流程：源文件、最大 10MB 的浏览版本和必要缩略图分别上传到私有频道，并在后台关联为同一逻辑图片的不同版本。
 - 源文件超过 Bot API 云端 `getFile` 下载上限时，浏览版本必须在上传前生成；VPS 不负责下载该源文件后再转换。
 - 图片缺少合规浏览版本、MIME 不符合要求、尺寸越界或 Telegram 资源不可读取时，不允许发布对应内容单元。
+- 每个已发布作品必须配置独立公开展示封面；该封面只能使用缩略图或浏览版本，不得关联图片源文件、会员正文图片或视频资源。
 
 ### 11.2 作品管理
 
@@ -444,6 +452,7 @@
 - 会员权限开关关闭时，所有已发布内容按公开内容处理。
 - 会员权限开关开启时，有效会员可访问所有已发布内容，普通用户只能访问公开内容。
 - 普通用户仍可看到会员作品安全基础资料和购买会员入口，但不会获得会员分区、内容单元或媒体访问能力。
+- 权限接口返回 `access_state` 三态，不再使用单一布尔值表达混合权限作品。
 - 查看视频发送记录和失败原因。
 - 管理员操作必须进入审计日志。
 
@@ -503,6 +512,7 @@
 - 推荐原始行为事件保留 180 天，随后删除；聚合兴趣画像按用户最后活动时间滚动更新。
 - 应用运行日志默认保留 30 天，鉴权与安全日志保留 90 天，管理员审计日志保留 365 天。
 - 用户删除账号后，行为事件和兴趣画像在 30 天内删除或匿名化；依法或为安全审计必须保留的记录只保留最小必要字段。
+- 会员入口与激活归因事件保留 180 天；短时入口标识保存哈希，不保存可复用的完整深链接参数。
 
 ## 13. MVP 范围
 
@@ -541,6 +551,11 @@
 - 下架内容不进入目录；无权访问的会员分区、内容单元和媒体不进入目录，会员作品只按锁定作品规则展示安全基础资料和购买入口。
 - 会员权限开启时，普通用户无法获得会员文件和媒体地址；有效会员可访问全部已发布内容。
 - 会员权限关闭时，已发布内容不再因会员级别被阻止。
+- 作品为 `member` 且子级未显式设置权限时，普通用户获得 `locked` 响应。
+- 作品为 `member` 且同时存在显式 `public` 与有效 `member` 子级时，普通用户获得 `partial` 响应并只能看到公开子级。
+- 作品为 `member` 且全部已发布子级均显式为 `public` 时，普通用户获得 `full` 响应。
+- 作品为 `public` 且部分子级为 `member` 时，普通用户获得 `partial` 响应并看不到会员子级。
+- 有效会员或会员权限关闭时，所有已发布内容返回 `full`。
 
 ### 14.2 图片阅读
 
@@ -578,6 +593,8 @@
 - 未填写的地区、年份、演员、作者等资料统一显示“未知”。
 - 一名具备发布权限的管理员可以独立完成入库审核和发布。
 - 所有单管理员发布操作均写入审计日志。
+- 打开会员入口后产生一次幂等 `membership_cta_open` 事件；管理员开通会员产生 `membership_activated` 事件，并按最近 30 天入口完成归因。
+- 锁定作品公开封面只能来自独立公开展示资源，响应中不出现会员正文或源文件地址。
 
 ## 15. 已确认产品决策
 
@@ -591,3 +608,6 @@
 8. 会员作品对普通用户保留安全基础资料、公开展示封面、会员标识和购买入口；会员分区、内容单元和媒体能力保持不可访问。
 9. Telegram Premium 4GB 视频链路已由过往项目验证通过，当前项目复用结论并只在 Staging 做冒烟回归。
 10. MVP 管理后台通过 Bot 的管理员专用 Web App 入口在 Telegram 内打开，使用 `initData`、管理员白名单和角色权限完成登录；首版不提供独立浏览器登录。
+11. 权限响应采用 `full`、`partial`、`locked` 三态，先计算子级权限，公开子级可以显式覆盖会员父级。
+12. 所有已发布作品必须配置独立公开展示封面，封面不复用受保护内容资源。
+13. 会员入口打开与管理员会员激活均由服务端记录，使用最近 30 天最后一次有效入口进行转化归因。
--- docs/technical-development.md
+++ docs/technical-development.md
@@ -4,10 +4,10 @@
 
 | 项目 | 内容 |
 |---|---|
-| 文档版本 | 1.2 |
+| 文档版本 | 1.3 |
 | 文档状态 | 已确认技术基线 |
 | 更新日期 | 2026-09-02 |
-| 对应需求文档 | `docs/requirements.md` 1.2 |
+| 对应需求文档 | `docs/requirements.md` 1.3 |
 | 架构形态 | 模块化单体 + 独立 Bot Worker |
 
 ## 2. 技术目标与约束
@@ -238,7 +238,7 @@
 - 作品 ID。
 - 主类型：`video`、`comic`、`gallery`、`photoshoot`。
 - 子类型：电影、电视剧、短剧、普通图集等。
-- 名称、别名、简介、封面资源 ID。
+- 名称、别名、简介、公开展示封面资源 ID `public_cover_asset_id`。
 - 通用可选资料：原名、别名、简介、地区或国家、发行年份、发行日期、语言、标签、发行状态和分级。
 - 影视可选资料：导演、演员、编剧、制片人、出品方、总集数和时长。
 - 漫画可选资料：作者、原作、作画、出版社、连载平台和连载状态。
@@ -281,13 +281,16 @@
 - 文件名、MIME、大小、宽高和时长。
 - 视频版本标识、像素数和管理员主版本标记。
 - 图片逻辑资源 ID、父资源 ID和版本角色：`source`、`browse` 或 `thumbnail`。
+- 展示范围 `presentation_scope`：`public_preview` 或 `protected_content`。
 - 页序或资源顺序。
 - 状态：待编排、可用、失效、下架。
 
 `storage_chat_id + source_message_id` 是 Telegram 源消息的主要定位依据；`file_id` 用于 Bot API 文件复用或图片获取。
 
-同一逻辑图片可以对应多个 `media_assets`。`source` 只用于受控管理和归档；`browse` 是 Mini App 放大查看使用的合规版本；`thumbnail` 用于列表和首屏。会员锁定作品卡片使用的封面必须是单独标记为公开展示的资源，不能复用会员图片地址。
+同一逻辑图片可以对应多个 `media_assets`。`source` 只用于受控管理和归档；`browse` 是 Mini App 放大查看使用的合规版本；`thumbnail` 用于列表和首屏。
 
+`works.public_cover_asset_id` 必须引用类型为图片或缩略图、版本角色为 `browse` 或 `thumbnail`、展示范围为 `public_preview` 的可用资源。它不得引用 `source`、视频、会员正文页或 `protected_content` 资源。公开封面仍通过后端图片代理返回，不暴露 Telegram 地址；访问时只验证作品可发现性，不执行会员媒体权限检查。
+
 #### content_relations
 
 - 来源作品或内容单元。
@@ -322,7 +325,23 @@
 不记录 Telegram 原生播放器的虚构播放完成事件。
 
 收藏、取消收藏、阅读进度和请求发送由对应业务接口在服务端生成事件，客户端不能直接伪造。`POST /events/content` 只接受展示和点击事件，并要求推荐请求 ID、结果归属校验、幂等去重和独立速率限制。原始事件保留 180 天，聚合画像按用户状态更新；账号删除后 30 天内删除或匿名化相关行为数据。
+
+#### membership_cta_tokens
+
+- Token 哈希和内部 Token ID，不保存可复用的完整深链接参数。
+- 绑定用户 ID、可选作品 ID、来源位置和可选推荐请求 ID。
+- 创建时间、失效时间和状态；MVP Token 有效期为 7 天。
+
+#### membership_conversion_events
+
+- 事件 ID、用户 ID和可选作品 ID。
+- 事件类型：`membership_cta_open` 或 `membership_activated`。
+- 来源位置、可选推荐请求 ID和 Token 哈希。
+- 事件时间、幂等键、可选操作管理员 ID。
+- `membership_activated` 的可选归因入口事件 ID。
 
+Bot 成功验证会员入口 Token 后以“用户 + Token + 事件类型”幂等记录 `membership_cta_open`。管理员把用户从非会员调整为有效会员时，会员领域服务记录 `membership_activated`，并关联该用户激活前最近 30 天内最后一次有效 `membership_cta_open`；没有有效入口时保留激活事件但归因字段为空。两类事件保留 180 天。
+
 #### user_interest_profiles
 
 - 用户 ID。
@@ -365,6 +384,8 @@
 - 会员权限关闭时跳过内容会员级别检查；开启时普通用户只能访问 `public` 内容。
 - 有效访问级别按“内容单元显式值 -> 分区显式值 -> 作品显式值 -> `public` 默认值”计算，最接近资源的显式值优先。
 - 作品发现权限与媒体访问权限分离：普通用户可发现锁定会员作品，但只获得安全基础资料、公开展示封面、会员标识和购买入口。
+- 作品响应状态由实际已发布子级计算为 `full`、`partial` 或 `locked`，不得仅根据作品级 `access_level` 提前结束查询。
+- 所有已发布作品必须具有满足 `public_cover_asset_id` 约束的公开展示封面。
 - 图片源文件不建立面向用户的 Bot 分发操作。
 - 视频分发从可用版本中选择像素数最高的版本；像素数相同时选择管理员主版本。
 - 已发布内容单元必须至少包含一个可用媒体资源。
@@ -379,12 +400,23 @@
 
 1. 检查作品发布状态；未发布或已下架作品对普通用户不可发现。
 2. 读取全局会员权限开关和当前用户会员状态。
-3. 计算作品有效访问级别和 `access_granted`。
-4. 当普通用户访问锁定会员作品时，返回锁定作品 DTO：安全基础资料、公开展示封面、会员标识和购买入口；不查询或返回分区、内容单元、资源数量、媒体地址和分发能力。
-5. 对可访问作品查询已配置分区，并按作品主类型过滤不允许的分区类型。
-6. 逐项计算分区和内容单元有效访问级别；会员开关开启时过滤普通用户无权访问的 `member` 内容。
-7. 过滤结果为空的分区，按后台配置顺序返回最终分区及内容摘要。
-8. 会员权限开关关闭时跳过会员过滤，所有已发布内容视为可访问，并隐藏会员锁定状态和购买入口。
+3. 查询作品全部已发布分区和内容单元，并按作品主类型过滤不允许的组合。
+4. 按“内容单元 -> 分区 -> 作品 -> `public`”计算每个内容单元的有效访问级别，不因作品级为 `member` 而提前返回。
+5. 会员权限关闭或当前用户为有效会员时，将所有已发布内容归入可访问集合，返回 `access_state=full`。
+6. 普通用户访问时，将有效级别为 `public` 的内容归入可访问集合，将 `member` 内容归入受限集合。
+7. 可访问集合包含全部已发布内容时返回 `full`；两个集合均非空时返回 `partial`；可访问集合为空时返回 `locked`。
+8. `full` 返回全部动态分区；`partial` 只按公开内容生成非空分区并返回“包含会员内容”标识和购买入口；`locked` 只返回锁定作品 DTO，不返回分区、内容单元、资源数量、媒体地址和分发能力。
+9. 所有状态均只使用 `public_cover_asset_id` 生成封面摘要或签名代理地址，禁止从受限内容推导封面。
+
+必须覆盖以下权限矩阵：
+
+| 作品 | 分区或单元 | 普通用户结果 |
+|---|---|---|
+| `member` | 未设置，继承 `member` | `locked` |
+| `member` | 至少一个显式 `public`，同时存在 `member` | `partial`，返回公开子级 |
+| `member` | 全部显式 `public` | `full` |
+| `public` | 同时存在 `public` 与 `member` | `partial`，过滤会员子级 |
+| 任意 | 会员开关关闭或有效会员 | `full` |
 
 不得把 `has_comic`、`has_photoshoot` 等人工维护布尔值作为事实来源。可以建立缓存或物化统计字段，但必须可由真实内容重新计算。
 
@@ -428,9 +460,9 @@
 - `GET /units/{unitId}`：章节、剧集或图集详情。
 - `GET /units/{unitId}/images`：按序返回签名图片资源摘要。
 
-作品列表和详情统一返回 `access_granted`、`access_level` 和可选 `membership_cta`。锁定作品响应使用独立 Schema，并禁止出现 `sections`、`units`、资源数量、媒体 URL、图片签名和视频分发参数。混合权限作品只返回当前用户可访问的分区和单元。
+作品列表和详情统一返回 `access_state`、作品配置的 `access_level`、`public_cover` 和可选 `membership_cta`。`access_state` 取值为 `full`、`partial` 或 `locked`，不再使用单一 `access_granted` 布尔值表达混合权限。锁定作品响应使用独立 Schema，并禁止出现 `sections`、`units`、资源数量、媒体 URL、正文图片签名和视频分发参数；允许返回 `public_cover` 的代理地址。`partial` 只返回当前用户可访问的分区和单元。
 
-`membership_cta` 使用受控 Bot 深链接和短来源标识，打开会员开通说明或人工服务流程。MVP 不创建订单或支付接口；管理员确认完成后通过管理后台手动设置会员状态和有效期。
+`membership_cta` 使用绑定当前用户、有效期 7 天的服务端短 Token 生成受控 Bot 深链接，记录来源位置、可选作品和推荐请求 ID。MVP 不创建订单或支付接口；管理员确认完成后通过管理后台手动设置会员状态和有效期，由会员领域服务生成激活事件并完成 30 天归因。
 
 ### 8.4 用户行为接口
 
@@ -474,6 +506,8 @@
 
 管理接口统一位于 `/admin` 命名空间并实施角色权限控制。
 
+管理员修改会员状态时必须通过会员领域服务执行。只有状态从无效变为有效时生成幂等 `membership_activated` 事件；重复保存相同会员状态不重复计数。管理员 ID、会员有效期变更和归因入口进入审计记录。
+
 ### 8.8 管理员身份
 
 - Bot 仅向 `admin_accounts` 白名单中的有效管理员返回专用 `web_app` 按钮。
@@ -535,6 +569,8 @@
 ### 10.3 用户会话与可发送状态
 
 - Bot 在收到用户 `/start` 时记录 `bot_started_at` 和最近可发送状态；`/start` 参数只保存短内部跳转标识。
+- 当 `/start` 参数为会员入口 Token 时，服务端验证 Token 存在、未过期且绑定当前 Telegram 用户；验证成功后幂等记录 `membership_cta_open`，再展示会员开通说明或人工服务入口。
+- 过期、无效或属于其他用户的会员入口 Token 不记录转化事件，也不暴露绑定作品或用户信息；Bot 可以返回不带归因的通用会员说明。
 - Mini App 创建视频任务前检查最近可发送状态。尚未启动 Bot 时返回稳定错误码和带原作品跳转参数的 Bot 深链接，不创建队列任务。
 - Telegram 返回 Bot 被屏蔽或目标聊天不可用时，更新用户可发送状态，任务直接失败且不进行无限重试。
 - 用户完成 `/start` 或解除屏蔽后可以重试；重试必须重新校验会员、内容发布状态和媒体可用性。
@@ -722,7 +758,8 @@
 
 - 用户接口验证业务会话和资源权限。
 - 会员开关开启时，所有作品、分区、单元、图片签名和视频发送请求统一执行 `public/member` 权限判断。
-- 对作品执行权限判断时同时返回 `access_granted`，不隐藏已发布会员作品的安全发现信息；对分区、单元、图片和视频分发则执行严格媒体访问过滤。
+- 对作品执行权限判断时返回 `access_state`，不隐藏已发布会员作品的安全发现信息；对分区、单元、图片和视频分发执行严格媒体访问过滤。
+- `public_cover_asset_id` 只允许访问 `public_preview` 资源；普通用户请求 `protected_content` 图片时仍必须通过内容权限校验。
 - 管理接口同时验证管理员身份和角色权限。
 - Bot 回调重新验证权限。
 - 存储频道只允许受控管理员和 Bot。
@@ -843,6 +880,7 @@
 - 推荐原始行为事件保留 180 天，超过期限批量删除；聚合兴趣画像按用户最后活动时间持续更新。
 - 应用运行日志保留 30 天，鉴权与安全日志保留 90 天，管理员审计日志保留 365 天。
 - 用户账号删除后 30 天内删除或匿名化行为事件与兴趣画像；必须保留的安全审计记录只保留最小必要标识。
+- `membership_cta_tokens` 到期后删除可解析绑定数据，仅按日志策略保留不可逆摘要；会员转化事件保留 180 天。
 - 保留任务由队列定期执行，执行数量、失败数量和删除数量进入监控。
 
 ## 17. 测试策略
@@ -858,8 +896,11 @@
 - 分发幂等键和重试分类。
 - 图片签名验证。
 - 作品、分区、内容单元三级权限继承和显式覆盖。
-- 锁定作品 DTO 不包含分区、单元、资源数量、媒体 URL 或分发参数。
+- `full`、`partial`、`locked` 状态及完整父子权限矩阵，特别覆盖 `member` 作品下显式 `public` 子级。
+- 锁定作品 DTO 不包含分区、单元、资源数量、正文媒体 URL 或分发参数，只允许公开封面代理地址。
+- `public_cover_asset_id` 类型、版本角色、展示范围和禁止引用源文件的约束。
 - 客户端事件归属校验、幂等去重和速率限制。
+- 会员入口 Token 的用户绑定、过期、幂等打开事件和 30 天激活归因。
 
 ### 17.2 集成测试
 
@@ -872,6 +913,8 @@
 - 管理员 Web App `initData`、白名单、角色和普通用户令牌隔离。
 - 未启动 Bot、已屏蔽、解除屏蔽后重试的视频分发状态。
 - 图片 `source`、`browse`、`thumbnail` 关联和发布门槛。
+- 会员父级下公开子级的查询结果、公开封面访问和受限正文隔离。
+- 管理员会员激活事件、重复保存幂等和无入口激活场景。
 
 ### 17.3 前端测试
 
@@ -883,6 +926,7 @@
 - 视频发送的排队、成功和失败状态。
 - 普通用户和会员用户的内容可见性差异。
 - 普通用户可见会员作品锁定卡片和购买入口，但网络响应不包含会员目录或媒体能力。
+- 混合权限作品显示公开分区并返回 `partial`，完全锁定作品返回 `locked`。
 - 客户端自适应预览尺寸选择。
 
 ### 17.4 真实 Telegram 验收
@@ -922,6 +966,7 @@
 - 实现频道入库和后台编排。
 - 实现动态目录、会员开关、访问级别和单管理员发布。
 - 实现锁定会员作品 DTO、公开展示封面和购买会员入口。
+- 实现三态权限算法、公开封面字段和会员转化事件。
 - 实现图片多版本编排和发布校验。
 
 ### 阶段 3：Mini App 浏览与图片阅读
@@ -946,7 +991,7 @@
 ### 18.1 阶段准入条件
 
 - 阶段 1 状态为 `READY`，可以立即启动，4GB 技术可行性不再构成阻断项。
-- 阶段 2 所需的三级权限字段、继承算法、锁定作品 Schema 和 `publication_status` / `release_status` 命名已经在本文件冻结，状态为 `READY`。
+- 阶段 2 所需的三级权限字段、三态算法、公开封面 Schema、会员转化事件和 `publication_status` / `release_status` 命名已经在本文件冻结，状态为 `READY`。
 - 阶段 3 的图片多版本流程已经冻结，状态为 `READY`；开发完成后使用 `source`、`browse`、`thumbnail` 入库样本验收。
 - 阶段 4 的 Bot 会话和分发流程已经冻结，状态为 `READY`；开发完成后在 Staging 验收 `/start`、屏蔽恢复和 4GB 冒烟回归。
 - 阶段 5 上线前通过容量、SLO、备份恢复、数据保留和跨客户端验收。
@@ -964,6 +1009,8 @@
 | 存储频道消息被删除 | 资源失效 | 管理权限最小化、定期一致性检查、失效告警 |
 | 4GB 当前环境配置回归 | 已验证链路可能因 Bot、频道或客户端环境变化失败 | 复用过往验证结论，Staging 使用真实 4GB 样本冒烟；失败按环境缺陷处理并阻断该环境上线 |
 | 空分区或权限泄漏 | 用户看到无效入口 | 服务端动态目录统一计算，前端不自行推断 |
+| 父级会员权限提前短路 | 显式公开子级被错误隐藏 | 先计算全部已发布子级，再生成 `full/partial/locked` 状态并执行矩阵测试 |
+| 公开封面误用受限资源 | 锁定作品泄露会员图片 | 独立 `public_cover_asset_id`、`public_preview` 约束和发布校验 |
 
 ## 20. 技术决策记录
 
@@ -1017,7 +1064,7 @@
 
 ### ADR-013：三级权限继承
 
-状态：接受。`works`、`content_sections` 和 `content_units` 均支持可选 `access_level`；有效值按内容单元、分区、作品、公开默认值的顺序解析，子级显式值覆盖父级。
+状态：接受。`works`、`content_sections` 和 `content_units` 均支持可选 `access_level`；有效值按内容单元、分区、作品、公开默认值的顺序解析，子级显式值覆盖父级。作品响应在计算全部子级后生成 `full`、`partial` 或 `locked`，不得按作品级权限提前短路。
 
 ### ADR-014：管理员 Telegram Web App 登录
 
@@ -1027,6 +1074,14 @@
 
 状态：接受。管理员在上传前生成 `browse` 和 `thumbnail` 版本，分别存入 Telegram 并与 `source` 关联；VPS 不承担图片转码或永久存储。
 
+### ADR-016：独立公开展示封面
+
+状态：接受。所有已发布作品使用 `public_cover_asset_id` 引用 `public_preview` 范围的 `browse` 或 `thumbnail` 资源；禁止引用源文件、视频、会员正文或受保护资源。
+
+### ADR-017：会员入口与激活归因
+
+状态：接受。会员入口使用绑定用户且 7 天有效的短 Token；Bot 记录幂等入口打开事件，管理员激活会员时由服务端记录激活事件并归因到最近 30 天最后一次有效入口。
+
 ## 21. 已确认开发基线
 
 1. 提供全局会员权限开关。开关开启时，有效会员可访问所有已发布文件，普通用户不能访问 `member` 文件；关闭时所有已发布内容按公开内容处理。
@@ -1041,10 +1096,13 @@
 10. 管理后台通过 Telegram 内管理员专用 Web App 登录；管理员本地生成图片浏览版本和缩略图，VPS 不转码。
 11. MVP 容量、SLO、RPO、RTO、日志与行为事件保留期以第 15、16 节为验收基线。
 12. 锁定作品的会员入口跳转 Bot 内开通说明或人工服务流程，管理员手动开通会员；MVP 不实现订单和支付。
+13. 权限响应使用 `full`、`partial`、`locked` 三态，服务端先计算全部已发布子级；显式公开子级可以覆盖会员父级。
+14. 所有已发布作品使用独立 `public_cover_asset_id`，只能引用 `public_preview` 的浏览版本或缩略图。
+15. 会员入口打开和会员激活均由服务端记录，按最近 30 天最后一次有效入口归因，原始转化事件保留 180 天。
 
 ## 22. 开发就绪结论
 
-- 产品范围、技术路线、三级权限、会员转化、Bot 会话、图片多版本、管理员登录、事件治理和非功能指标均已冻结。
+- 产品范围、技术路线、三态权限、公开封面、会员转化、Bot 会话、图片多版本、管理员登录、事件治理和非功能指标均已冻结。
 - Telegram Premium 4GB 视频链路采用过往项目已通过的验证结论，不存在待确认的技术可行性阻断。
 - 阶段 1 至阶段 4 均已具备开发输入，可以按第 18 节顺序实施；各阶段完成后执行对应测试，不需要再次进行产品决策。
 - 当前项目开发状态为 `GO`。生产上线仍必须通过 Staging、容量、安全、备份恢复和跨客户端验收，这些属于发布质量门槛，不影响开始开发。

